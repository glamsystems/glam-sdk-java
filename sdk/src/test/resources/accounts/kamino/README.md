# Kamino mainnet account fixtures

Copies of the snapshots documented in
`services/src/test/resources/accounts/kamino/README.md` (same keys, same
provenance), shared into the sdk suite for `VaultTableBuilder` coverage:
the VaultState `5YxwKgsv...` and the SOL Reserve `d4A2prbA...`.
