// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.loopscale.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// @param market Loopscale market information account this policy applies to.
/// @param maxBorrowAmount Maximum principal amount that may be borrowed in a single instruction.
///                        A value of zero means no per-instruction borrow is allowed for this market.
/// @param maxTotalBorrowAmount Maximum outstanding principal a single loan may owe in this market.
///                             Enforced per loan (summed over that loan's ledgers in this market), not
///                             aggregated across all of the vault's loans in this market. A value of zero
///                             means no outstanding borrow exposure is allowed for this market.
/// @param maxLtvBps Maximum expected liquidation LTV threshold, in basis points.
///                  Borrow enforcement compares this to Loopscale's expected LQT values.
///                  A value of zero means no expected liquidation LTV is allowed for this market.
/// @param durationIndexesAllowlist Duration indexes that may be used when borrowing from this market.
///                                 An empty list denies all borrow durations for this market. Index mapping:
///                                 - 0 = 1 day
///                                 - 1 = 1 week
///                                 - 2 = 1 month
///                                 - 3 = 3 months
///                                 - 4 = 1 year
public record BorrowMarketPolicy(PublicKey market,
                                 long maxBorrowAmount,
                                 long maxTotalBorrowAmount,
                                 int maxLtvBps,
                                 byte[] durationIndexesAllowlist) implements SerDe {

  public static final int MARKET_OFFSET = 0;
  public static final int MAX_BORROW_AMOUNT_OFFSET = 32;
  public static final int MAX_TOTAL_BORROW_AMOUNT_OFFSET = 40;
  public static final int MAX_LTV_BPS_OFFSET = 48;
  public static final int DURATION_INDEXES_ALLOWLIST_OFFSET = 50;

  public static BorrowMarketPolicy read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var market = readPubKey(_data, i);
    i += 32;
    final var maxBorrowAmount = getInt64LE(_data, i);
    i += 8;
    final var maxTotalBorrowAmount = getInt64LE(_data, i);
    i += 8;
    final var maxLtvBps = getInt16LE(_data, i);
    i += 2;
    final var durationIndexesAllowlist = SerDeUtil.readbyteVector(4, _data, i);
    return new BorrowMarketPolicy(market,
                                  maxBorrowAmount,
                                  maxTotalBorrowAmount,
                                  maxLtvBps,
                                  durationIndexesAllowlist);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    market.write(_data, i);
    i += 32;
    putInt64LE(_data, i, maxBorrowAmount);
    i += 8;
    putInt64LE(_data, i, maxTotalBorrowAmount);
    i += 8;
    putInt16LE(_data, i, maxLtvBps);
    i += 2;
    i += SerDeUtil.writeVector(4, durationIndexesAllowlist, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return 32
         + 8
         + 8
         + 2
         + SerDeUtil.lenVector(4, durationIndexesAllowlist);
  }
}
