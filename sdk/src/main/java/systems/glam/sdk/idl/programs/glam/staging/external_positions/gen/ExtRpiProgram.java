// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.external_positions.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import systems.glam.sdk.idl.programs.glam.staging.external_positions.gen.types.PositionConfig;
import systems.glam.sdk.idl.programs.glam.staging.external_positions.gen.types.PositionObservationInput;
import systems.glam.sdk.idl.programs.glam.staging.external_positions.gen.types.WormholeHyperliquidObservationConfigInput;
import systems.glam.sdk.idl.programs.glam.staging.external_positions.gen.types.WormholeObservationConfigInput;

import java.util.List;

import static java.util.Objects.requireNonNullElse;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createReadOnlySigner;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class ExtRpiProgram {

  public static final Discriminator REMOVE_REGISTERED_POSITION_DISCRIMINATOR = toDiscriminator(102, 188, 200, 90, 244, 35, 5, 7);

  /// Remove a registered position from the registry.
  /// Closes the observation state PDA and refunds rent to the signer.
  ///
  public static List<AccountMeta> removeRegisteredPositionKeys(final SolanaAccounts solanaAccounts,
                                                               final PublicKey glamStateKey,
                                                               final PublicKey glamSignerKey,
                                                               final PublicKey glamVaultKey,
                                                               final PublicKey observationStateKey,
                                                               final PublicKey integrationAuthorityKey,
                                                               final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWritableSigner(glamSignerKey),
      createWrite(glamVaultKey),
      createWrite(observationStateKey),
      createRead(integrationAuthorityKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Remove a registered position from the registry.
  /// Closes the observation state PDA and refunds rent to the signer.
  ///
  public static Instruction removeRegisteredPosition(final AccountMeta invokedExtRpiProgramMeta,
                                                     final SolanaAccounts solanaAccounts,
                                                     final PublicKey glamStateKey,
                                                     final PublicKey glamSignerKey,
                                                     final PublicKey glamVaultKey,
                                                     final PublicKey observationStateKey,
                                                     final PublicKey integrationAuthorityKey,
                                                     final PublicKey glamProtocolProgramKey,
                                                     final byte[] positionId) {
    final var keys = removeRegisteredPositionKeys(
      solanaAccounts,
      glamStateKey,
      glamSignerKey,
      glamVaultKey,
      observationStateKey,
      integrationAuthorityKey,
      glamProtocolProgramKey
    );
    return removeRegisteredPosition(invokedExtRpiProgramMeta, keys, positionId);
  }

  /// Remove a registered position from the registry.
  /// Closes the observation state PDA and refunds rent to the signer.
  ///
  public static Instruction removeRegisteredPosition(final AccountMeta invokedExtRpiProgramMeta,
                                                     final List<AccountMeta> keys,
                                                     final byte[] positionId) {
    final byte[] _data = new byte[8 + SerDeUtil.lenArray(positionId)];
    int i = REMOVE_REGISTERED_POSITION_DISCRIMINATOR.write(_data, 0);
    SerDeUtil.writeArrayChecked(positionId, 32, _data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record RemoveRegisteredPositionIxData(Discriminator discriminator, byte[] positionId) implements SerDe {  

    public static RemoveRegisteredPositionIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 40;
    public static final int POSITION_ID_LEN = 32;

    public static final int POSITION_ID_OFFSET = 8;

    public static RemoveRegisteredPositionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var positionId = new byte[32];
      SerDeUtil.readArray(positionId, _data, i);
      return new RemoveRegisteredPositionIxData(discriminator, positionId);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeArrayChecked(positionId, 32, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SUBMIT_OBSERVATION_DISCRIMINATOR = toDiscriminator(109, 4, 22, 163, 138, 215, 205, 180);

  /// Submit an observation for a configured registered position.
  /// Writes to pending slot; replaces any existing pending observation.
  ///
  public static List<AccountMeta> submitObservationKeys(final PublicKey glamStateKey,
                                                        final PublicKey glamSignerKey,
                                                        final PublicKey observationStateKey) {
    return List.of(
      createRead(glamStateKey),
      createReadOnlySigner(glamSignerKey),
      createWrite(observationStateKey)
    );
  }

  /// Submit an observation for a configured registered position.
  /// Writes to pending slot; replaces any existing pending observation.
  ///
  public static Instruction submitObservation(final AccountMeta invokedExtRpiProgramMeta,
                                              final PublicKey glamStateKey,
                                              final PublicKey glamSignerKey,
                                              final PublicKey observationStateKey,
                                              final PositionObservationInput input) {
    final var keys = submitObservationKeys(
      glamStateKey,
      glamSignerKey,
      observationStateKey
    );
    return submitObservation(invokedExtRpiProgramMeta, keys, input);
  }

  /// Submit an observation for a configured registered position.
  /// Writes to pending slot; replaces any existing pending observation.
  ///
  public static Instruction submitObservation(final AccountMeta invokedExtRpiProgramMeta,
                                              final List<AccountMeta> keys,
                                              final PositionObservationInput input) {
    final byte[] _data = new byte[8 + input.l()];
    int i = SUBMIT_OBSERVATION_DISCRIMINATOR.write(_data, 0);
    input.write(_data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record SubmitObservationIxData(Discriminator discriminator, PositionObservationInput input) implements SerDe {  

    public static SubmitObservationIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 233;

    public static final int INPUT_OFFSET = 8;

    public static SubmitObservationIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var input = PositionObservationInput.read(_data, i);
      return new SubmitObservationIxData(discriminator, input);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += input.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SUBMIT_OBSERVATION_WORMHOLE_DISCRIMINATOR = toDiscriminator(98, 36, 143, 5, 50, 123, 9, 67);

  /// Submit a Wormhole Guardian-verified registered observation.
  /// The caller only relays a VAA body whose signatures have already been
  /// posted to the Wormhole Verification Shim.
  ///
  public static List<AccountMeta> submitObservationWormholeKeys(final PublicKey glamStateKey,
                                                                final PublicKey glamSignerKey,
                                                                final PublicKey observationStateKey,
                                                                final PublicKey wormholeConfigKey,
                                                                final PublicKey guardianSetKey,
                                                                final PublicKey guardianSignaturesKey,
                                                                final PublicKey wormholeVerifyVaaShimKey) {
    return List.of(
      createRead(glamStateKey),
      createWritableSigner(glamSignerKey),
      createWrite(observationStateKey),
      createWrite(wormholeConfigKey),
      createRead(guardianSetKey),
      createRead(guardianSignaturesKey),
      createRead(wormholeVerifyVaaShimKey)
    );
  }

  /// Submit a Wormhole Guardian-verified registered observation.
  /// The caller only relays a VAA body whose signatures have already been
  /// posted to the Wormhole Verification Shim.
  ///
  public static Instruction submitObservationWormhole(final AccountMeta invokedExtRpiProgramMeta,
                                                      final PublicKey glamStateKey,
                                                      final PublicKey glamSignerKey,
                                                      final PublicKey observationStateKey,
                                                      final PublicKey wormholeConfigKey,
                                                      final PublicKey guardianSetKey,
                                                      final PublicKey guardianSignaturesKey,
                                                      final PublicKey wormholeVerifyVaaShimKey,
                                                      final byte[] positionId,
                                                      final int guardianSetBump,
                                                      final byte[] vaaBody) {
    final var keys = submitObservationWormholeKeys(
      glamStateKey,
      glamSignerKey,
      observationStateKey,
      wormholeConfigKey,
      guardianSetKey,
      guardianSignaturesKey,
      wormholeVerifyVaaShimKey
    );
    return submitObservationWormhole(
      invokedExtRpiProgramMeta,
      keys,
      positionId,
      guardianSetBump,
      vaaBody
    );
  }

  /// Submit a Wormhole Guardian-verified registered observation.
  /// The caller only relays a VAA body whose signatures have already been
  /// posted to the Wormhole Verification Shim.
  ///
  public static Instruction submitObservationWormhole(final AccountMeta invokedExtRpiProgramMeta,
                                                      final List<AccountMeta> keys,
                                                      final byte[] positionId,
                                                      final int guardianSetBump,
                                                      final byte[] vaaBody) {
    final byte[] _data = new byte[9 + SerDeUtil.lenArray(positionId) + SerDeUtil.lenVector(4, vaaBody)];
    int i = SUBMIT_OBSERVATION_WORMHOLE_DISCRIMINATOR.write(_data, 0);
    i += SerDeUtil.writeArrayChecked(positionId, 32, _data, i);
    _data[i] = (byte) guardianSetBump;
    ++i;
    SerDeUtil.writeVector(4, vaaBody, _data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record SubmitObservationWormholeIxData(Discriminator discriminator,
                                                byte[] positionId,
                                                int guardianSetBump,
                                                byte[] vaaBody) implements SerDe {  

    public static SubmitObservationWormholeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int POSITION_ID_LEN = 32;
    public static final int POSITION_ID_OFFSET = 8;
    public static final int GUARDIAN_SET_BUMP_OFFSET = 40;
    public static final int VAA_BODY_OFFSET = 41;

    public static SubmitObservationWormholeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var positionId = new byte[32];
      i += SerDeUtil.readArray(positionId, _data, i);
      final var guardianSetBump = _data[i] & 0xFF;
      ++i;
      final var vaaBody = SerDeUtil.readbyteVector(4, _data, i);
      return new SubmitObservationWormholeIxData(discriminator, positionId, guardianSetBump, vaaBody);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeArrayChecked(positionId, 32, _data, i);
      _data[i] = (byte) guardianSetBump;
      ++i;
      i += SerDeUtil.writeVector(4, vaaBody, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + SerDeUtil.lenArray(positionId) + 1 + SerDeUtil.lenVector(4, vaaBody);
    }
  }

  public static final Discriminator UPSERT_REGISTERED_POSITION_DISCRIMINATOR = toDiscriminator(89, 19, 168, 143, 107, 179, 125, 11);

  /// Create or update a registered position configuration.
  /// Creates the observation state PDA on first call for a position.
  ///
  public static List<AccountMeta> upsertRegisteredPositionKeys(final SolanaAccounts solanaAccounts,
                                                               final PublicKey glamStateKey,
                                                               final PublicKey glamSignerKey,
                                                               final PublicKey glamVaultKey,
                                                               final PublicKey observationStateKey,
                                                               final PublicKey integrationAuthorityKey,
                                                               final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWritableSigner(glamSignerKey),
      createWrite(glamVaultKey),
      createWrite(observationStateKey),
      createRead(integrationAuthorityKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Create or update a registered position configuration.
  /// Creates the observation state PDA on first call for a position.
  ///
  public static Instruction upsertRegisteredPosition(final AccountMeta invokedExtRpiProgramMeta,
                                                     final SolanaAccounts solanaAccounts,
                                                     final PublicKey glamStateKey,
                                                     final PublicKey glamSignerKey,
                                                     final PublicKey glamVaultKey,
                                                     final PublicKey observationStateKey,
                                                     final PublicKey integrationAuthorityKey,
                                                     final PublicKey glamProtocolProgramKey,
                                                     final PositionConfig config) {
    final var keys = upsertRegisteredPositionKeys(
      solanaAccounts,
      glamStateKey,
      glamSignerKey,
      glamVaultKey,
      observationStateKey,
      integrationAuthorityKey,
      glamProtocolProgramKey
    );
    return upsertRegisteredPosition(invokedExtRpiProgramMeta, keys, config);
  }

  /// Create or update a registered position configuration.
  /// Creates the observation state PDA on first call for a position.
  ///
  public static Instruction upsertRegisteredPosition(final AccountMeta invokedExtRpiProgramMeta,
                                                     final List<AccountMeta> keys,
                                                     final PositionConfig config) {
    final byte[] _data = new byte[8 + config.l()];
    int i = UPSERT_REGISTERED_POSITION_DISCRIMINATOR.write(_data, 0);
    config.write(_data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record UpsertRegisteredPositionIxData(Discriminator discriminator, PositionConfig config) implements SerDe {  

    public static UpsertRegisteredPositionIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int CONFIG_OFFSET = 8;

    public static UpsertRegisteredPositionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var config = PositionConfig.read(_data, i);
      return new UpsertRegisteredPositionIxData(discriminator, config);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += config.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + config.l();
    }
  }

  public static final Discriminator UPSERT_REGISTERED_POSITION_WORMHOLE_CONFIG_DISCRIMINATOR = toDiscriminator(139, 183, 73, 75, 245, 117, 75, 109);

  /// Create or update Wormhole verification config for a Wormhole-sourced
  /// registered position.
  ///
  public static List<AccountMeta> upsertRegisteredPositionWormholeConfigKeys(final SolanaAccounts solanaAccounts,
                                                                             final PublicKey glamStateKey,
                                                                             final PublicKey glamSignerKey,
                                                                             final PublicKey wormholeConfigKey) {
    return List.of(
      createRead(glamStateKey),
      createWritableSigner(glamSignerKey),
      createWrite(wormholeConfigKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Create or update Wormhole verification config for a Wormhole-sourced
  /// registered position.
  ///
  public static Instruction upsertRegisteredPositionWormholeConfig(final AccountMeta invokedExtRpiProgramMeta,
                                                                   final SolanaAccounts solanaAccounts,
                                                                   final PublicKey glamStateKey,
                                                                   final PublicKey glamSignerKey,
                                                                   final PublicKey wormholeConfigKey,
                                                                   final WormholeObservationConfigInput input) {
    final var keys = upsertRegisteredPositionWormholeConfigKeys(
      solanaAccounts,
      glamStateKey,
      glamSignerKey,
      wormholeConfigKey
    );
    return upsertRegisteredPositionWormholeConfig(invokedExtRpiProgramMeta, keys, input);
  }

  /// Create or update Wormhole verification config for a Wormhole-sourced
  /// registered position.
  ///
  public static Instruction upsertRegisteredPositionWormholeConfig(final AccountMeta invokedExtRpiProgramMeta,
                                                                   final List<AccountMeta> keys,
                                                                   final WormholeObservationConfigInput input) {
    final byte[] _data = new byte[8 + input.l()];
    int i = UPSERT_REGISTERED_POSITION_WORMHOLE_CONFIG_DISCRIMINATOR.write(_data, 0);
    input.write(_data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record UpsertRegisteredPositionWormholeConfigIxData(Discriminator discriminator, WormholeObservationConfigInput input) implements SerDe {  

    public static UpsertRegisteredPositionWormholeConfigIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 80;

    public static final int INPUT_OFFSET = 8;

    public static UpsertRegisteredPositionWormholeConfigIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var input = WormholeObservationConfigInput.read(_data, i);
      return new UpsertRegisteredPositionWormholeConfigIxData(discriminator, input);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += input.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UPSERT_REGISTERED_POSITION_WORMHOLE_HYPERLIQUID_CONFIG_DISCRIMINATOR = toDiscriminator(163, 241, 249, 72, 43, 248, 96, 44);

  /// Create or update Hyperliquid-specific payload config for a
  /// Wormhole-sourced registered position.
  ///
  public static List<AccountMeta> upsertRegisteredPositionWormholeHyperliquidConfigKeys(final SolanaAccounts solanaAccounts,
                                                                                        final PublicKey glamStateKey,
                                                                                        final PublicKey glamSignerKey,
                                                                                        final PublicKey wormholeConfigKey,
                                                                                        final PublicKey hyperliquidConfigKey) {
    return List.of(
      createRead(glamStateKey),
      createWritableSigner(glamSignerKey),
      createRead(wormholeConfigKey),
      createWrite(hyperliquidConfigKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Create or update Hyperliquid-specific payload config for a
  /// Wormhole-sourced registered position.
  ///
  public static Instruction upsertRegisteredPositionWormholeHyperliquidConfig(final AccountMeta invokedExtRpiProgramMeta,
                                                                              final SolanaAccounts solanaAccounts,
                                                                              final PublicKey glamStateKey,
                                                                              final PublicKey glamSignerKey,
                                                                              final PublicKey wormholeConfigKey,
                                                                              final PublicKey hyperliquidConfigKey,
                                                                              final WormholeHyperliquidObservationConfigInput input) {
    final var keys = upsertRegisteredPositionWormholeHyperliquidConfigKeys(
      solanaAccounts,
      glamStateKey,
      glamSignerKey,
      wormholeConfigKey,
      hyperliquidConfigKey
    );
    return upsertRegisteredPositionWormholeHyperliquidConfig(invokedExtRpiProgramMeta, keys, input);
  }

  /// Create or update Hyperliquid-specific payload config for a
  /// Wormhole-sourced registered position.
  ///
  public static Instruction upsertRegisteredPositionWormholeHyperliquidConfig(final AccountMeta invokedExtRpiProgramMeta,
                                                                              final List<AccountMeta> keys,
                                                                              final WormholeHyperliquidObservationConfigInput input) {
    final byte[] _data = new byte[8 + input.l()];
    int i = UPSERT_REGISTERED_POSITION_WORMHOLE_HYPERLIQUID_CONFIG_DISCRIMINATOR.write(_data, 0);
    input.write(_data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record UpsertRegisteredPositionWormholeHyperliquidConfigIxData(Discriminator discriminator, WormholeHyperliquidObservationConfigInput input) implements SerDe {  

    public static UpsertRegisteredPositionWormholeHyperliquidConfigIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 112;

    public static final int INPUT_OFFSET = 8;

    public static UpsertRegisteredPositionWormholeHyperliquidConfigIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var input = WormholeHyperliquidObservationConfigInput.read(_data, i);
      return new UpsertRegisteredPositionWormholeHyperliquidConfigIxData(discriminator, input);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += input.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator VALIDATE_OBSERVATION_DISCRIMINATOR = toDiscriminator(134, 150, 250, 52, 188, 156, 130, 122);

  /// Validate a pending observation and promote it to active.
  /// 
  /// Remaining accounts:
  /// - `remaining_accounts0` is required when the pending observation
  /// denomination is a non-base mint; it must be the observed mint oracle
  /// account used for price normalization.
  ///
  /// @param glamConfigKey and its discriminator is checked by AssetMetasRef before use.
  /// @param solUsdOracleKey against GLAM global oracle metadata before price use.
  /// @param baseAssetOracleKey GLAM global oracle metadata before price use.
  public static List<AccountMeta> validateObservationKeys(final AccountMeta invokedExtRpiProgramMeta,
                                                          final PublicKey glamStateKey,
                                                          final PublicKey glamSignerKey,
                                                          final PublicKey observationStateKey,
                                                          final PublicKey glamConfigKey,
                                                          final PublicKey solUsdOracleKey,
                                                          final PublicKey baseAssetOracleKey) {
    return List.of(
      createWrite(glamStateKey),
      createReadOnlySigner(glamSignerKey),
      createWrite(observationStateKey),
      createRead(requireNonNullElse(glamConfigKey, invokedExtRpiProgramMeta.publicKey())),
      createRead(requireNonNullElse(solUsdOracleKey, invokedExtRpiProgramMeta.publicKey())),
      createRead(requireNonNullElse(baseAssetOracleKey, invokedExtRpiProgramMeta.publicKey()))
    );
  }

  /// Validate a pending observation and promote it to active.
  /// 
  /// Remaining accounts:
  /// - `remaining_accounts0` is required when the pending observation
  /// denomination is a non-base mint; it must be the observed mint oracle
  /// account used for price normalization.
  ///
  /// @param glamConfigKey and its discriminator is checked by AssetMetasRef before use.
  /// @param solUsdOracleKey against GLAM global oracle metadata before price use.
  /// @param baseAssetOracleKey GLAM global oracle metadata before price use.
  public static Instruction validateObservation(final AccountMeta invokedExtRpiProgramMeta,
                                                final PublicKey glamStateKey,
                                                final PublicKey glamSignerKey,
                                                final PublicKey observationStateKey,
                                                final PublicKey glamConfigKey,
                                                final PublicKey solUsdOracleKey,
                                                final PublicKey baseAssetOracleKey,
                                                final byte[] positionId) {
    final var keys = validateObservationKeys(
      invokedExtRpiProgramMeta,
      glamStateKey,
      glamSignerKey,
      observationStateKey,
      glamConfigKey,
      solUsdOracleKey,
      baseAssetOracleKey
    );
    return validateObservation(invokedExtRpiProgramMeta, keys, positionId);
  }

  /// Validate a pending observation and promote it to active.
  /// 
  /// Remaining accounts:
  /// - `remaining_accounts0` is required when the pending observation
  /// denomination is a non-base mint; it must be the observed mint oracle
  /// account used for price normalization.
  ///
  public static Instruction validateObservation(final AccountMeta invokedExtRpiProgramMeta,
                                                final List<AccountMeta> keys,
                                                final byte[] positionId) {
    final byte[] _data = new byte[8 + SerDeUtil.lenArray(positionId)];
    int i = VALIDATE_OBSERVATION_DISCRIMINATOR.write(_data, 0);
    SerDeUtil.writeArrayChecked(positionId, 32, _data, i);

    return Instruction.createInstruction(invokedExtRpiProgramMeta, keys, _data);
  }

  public record ValidateObservationIxData(Discriminator discriminator, byte[] positionId) implements SerDe {  

    public static ValidateObservationIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 40;
    public static final int POSITION_ID_LEN = 32;

    public static final int POSITION_ID_OFFSET = 8;

    public static ValidateObservationIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var positionId = new byte[32];
      SerDeUtil.readArray(positionId, _data, i);
      return new ValidateObservationIxData(discriminator, positionId);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += SerDeUtil.writeArrayChecked(positionId, 32, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private ExtRpiProgram() {
  }
}
