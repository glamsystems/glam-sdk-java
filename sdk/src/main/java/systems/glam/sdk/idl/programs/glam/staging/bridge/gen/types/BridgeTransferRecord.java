// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.bridge.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.*;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record BridgeTransferRecord(PublicKey _address,
                                   Discriminator discriminator,
                                   PublicKey glamState,
                                   byte[] transferId,
                                   int protocol,
                                   BridgeTransferStatus status,
                                   boolean managed,
                                   boolean receiptVerified,
                                   PublicKey sourceMint,
                                   PublicKey sourceTokenAccount,
                                   PublicKey providerProgram,
                                   PublicKey providerConfig,
                                   long sourceAmount,
                                   long quotedOutAmount,
                                   int destinationChain,
                                   PublicKey destinationRecipient,
                                   byte[] providerTransferId,
                                   PublicKey providerEmitter,
                                   long providerSequence,
                                   long committedSlot,
                                   long settledSlot,
                                   long reconciledSlot,
                                   int failureReason,
                                   int bump) implements SerDe {

  public static final int BYTES = 353;
  public static final int TRANSFER_ID_LEN = 32;
  public static final int PROVIDER_TRANSFER_ID_LEN = 32;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(46, 131, 253, 32, 218, 137, 45, 96);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int GLAM_STATE_OFFSET = 8;
  public static final int TRANSFER_ID_OFFSET = 40;
  public static final int PROTOCOL_OFFSET = 72;
  public static final int STATUS_OFFSET = 74;
  public static final int MANAGED_OFFSET = 75;
  public static final int RECEIPT_VERIFIED_OFFSET = 76;
  public static final int SOURCE_MINT_OFFSET = 77;
  public static final int SOURCE_TOKEN_ACCOUNT_OFFSET = 109;
  public static final int PROVIDER_PROGRAM_OFFSET = 141;
  public static final int PROVIDER_CONFIG_OFFSET = 173;
  public static final int SOURCE_AMOUNT_OFFSET = 205;
  public static final int QUOTED_OUT_AMOUNT_OFFSET = 213;
  public static final int DESTINATION_CHAIN_OFFSET = 221;
  public static final int DESTINATION_RECIPIENT_OFFSET = 223;
  public static final int PROVIDER_TRANSFER_ID_OFFSET = 255;
  public static final int PROVIDER_EMITTER_OFFSET = 287;
  public static final int PROVIDER_SEQUENCE_OFFSET = 319;
  public static final int COMMITTED_SLOT_OFFSET = 327;
  public static final int SETTLED_SLOT_OFFSET = 335;
  public static final int RECONCILED_SLOT_OFFSET = 343;
  public static final int FAILURE_REASON_OFFSET = 351;
  public static final int BUMP_OFFSET = 352;

  public static Filter createGlamStateFilter(final PublicKey glamState) {
    return Filter.createMemCompFilter(GLAM_STATE_OFFSET, glamState);
  }

  public static Filter createProtocolFilter(final int protocol) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, protocol);
    return Filter.createMemCompFilter(PROTOCOL_OFFSET, _data);
  }

  public static Filter createStatusFilter(final BridgeTransferStatus status) {
    return Filter.createMemCompFilter(STATUS_OFFSET, status.write());
  }

  public static Filter createManagedFilter(final boolean managed) {
    return Filter.createMemCompFilter(MANAGED_OFFSET, new byte[]{(byte) (managed ? 1 : 0)});
  }

  public static Filter createReceiptVerifiedFilter(final boolean receiptVerified) {
    return Filter.createMemCompFilter(RECEIPT_VERIFIED_OFFSET, new byte[]{(byte) (receiptVerified ? 1 : 0)});
  }

  public static Filter createSourceMintFilter(final PublicKey sourceMint) {
    return Filter.createMemCompFilter(SOURCE_MINT_OFFSET, sourceMint);
  }

  public static Filter createSourceTokenAccountFilter(final PublicKey sourceTokenAccount) {
    return Filter.createMemCompFilter(SOURCE_TOKEN_ACCOUNT_OFFSET, sourceTokenAccount);
  }

  public static Filter createProviderProgramFilter(final PublicKey providerProgram) {
    return Filter.createMemCompFilter(PROVIDER_PROGRAM_OFFSET, providerProgram);
  }

  public static Filter createProviderConfigFilter(final PublicKey providerConfig) {
    return Filter.createMemCompFilter(PROVIDER_CONFIG_OFFSET, providerConfig);
  }

  public static Filter createSourceAmountFilter(final long sourceAmount) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, sourceAmount);
    return Filter.createMemCompFilter(SOURCE_AMOUNT_OFFSET, _data);
  }

  public static Filter createQuotedOutAmountFilter(final long quotedOutAmount) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, quotedOutAmount);
    return Filter.createMemCompFilter(QUOTED_OUT_AMOUNT_OFFSET, _data);
  }

  public static Filter createDestinationChainFilter(final int destinationChain) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, destinationChain);
    return Filter.createMemCompFilter(DESTINATION_CHAIN_OFFSET, _data);
  }

  public static Filter createDestinationRecipientFilter(final PublicKey destinationRecipient) {
    return Filter.createMemCompFilter(DESTINATION_RECIPIENT_OFFSET, destinationRecipient);
  }

  public static Filter createProviderEmitterFilter(final PublicKey providerEmitter) {
    return Filter.createMemCompFilter(PROVIDER_EMITTER_OFFSET, providerEmitter);
  }

  public static Filter createProviderSequenceFilter(final long providerSequence) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, providerSequence);
    return Filter.createMemCompFilter(PROVIDER_SEQUENCE_OFFSET, _data);
  }

  public static Filter createCommittedSlotFilter(final long committedSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, committedSlot);
    return Filter.createMemCompFilter(COMMITTED_SLOT_OFFSET, _data);
  }

  public static Filter createSettledSlotFilter(final long settledSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, settledSlot);
    return Filter.createMemCompFilter(SETTLED_SLOT_OFFSET, _data);
  }

  public static Filter createReconciledSlotFilter(final long reconciledSlot) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, reconciledSlot);
    return Filter.createMemCompFilter(RECONCILED_SLOT_OFFSET, _data);
  }

  public static Filter createFailureReasonFilter(final int failureReason) {
    return Filter.createMemCompFilter(FAILURE_REASON_OFFSET, new byte[]{(byte) failureReason});
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static BridgeTransferRecord read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static BridgeTransferRecord read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static BridgeTransferRecord read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], BridgeTransferRecord> FACTORY = BridgeTransferRecord::read;

  public static BridgeTransferRecord read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var glamState = readPubKey(_data, i);
    i += 32;
    final var transferId = new byte[32];
    i += SerDeUtil.readArray(transferId, _data, i);
    final var protocol = getInt16LE(_data, i);
    i += 2;
    final var status = BridgeTransferStatus.read(_data, i);
    i += status.l();
    final var managed = _data[i] == 1;
    ++i;
    final var receiptVerified = _data[i] == 1;
    ++i;
    final var sourceMint = readPubKey(_data, i);
    i += 32;
    final var sourceTokenAccount = readPubKey(_data, i);
    i += 32;
    final var providerProgram = readPubKey(_data, i);
    i += 32;
    final var providerConfig = readPubKey(_data, i);
    i += 32;
    final var sourceAmount = getInt64LE(_data, i);
    i += 8;
    final var quotedOutAmount = getInt64LE(_data, i);
    i += 8;
    final var destinationChain = getInt16LE(_data, i);
    i += 2;
    final var destinationRecipient = readPubKey(_data, i);
    i += 32;
    final var providerTransferId = new byte[32];
    i += SerDeUtil.readArray(providerTransferId, _data, i);
    final var providerEmitter = readPubKey(_data, i);
    i += 32;
    final var providerSequence = getInt64LE(_data, i);
    i += 8;
    final var committedSlot = getInt64LE(_data, i);
    i += 8;
    final var settledSlot = getInt64LE(_data, i);
    i += 8;
    final var reconciledSlot = getInt64LE(_data, i);
    i += 8;
    final var failureReason = _data[i] & 0xFF;
    ++i;
    final var bump = _data[i] & 0xFF;
    return new BridgeTransferRecord(_address,
                                    discriminator,
                                    glamState,
                                    transferId,
                                    protocol,
                                    status,
                                    managed,
                                    receiptVerified,
                                    sourceMint,
                                    sourceTokenAccount,
                                    providerProgram,
                                    providerConfig,
                                    sourceAmount,
                                    quotedOutAmount,
                                    destinationChain,
                                    destinationRecipient,
                                    providerTransferId,
                                    providerEmitter,
                                    providerSequence,
                                    committedSlot,
                                    settledSlot,
                                    reconciledSlot,
                                    failureReason,
                                    bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    glamState.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(transferId, 32, _data, i);
    putInt16LE(_data, i, protocol);
    i += 2;
    i += status.write(_data, i);
    _data[i] = (byte) (managed ? 1 : 0);
    ++i;
    _data[i] = (byte) (receiptVerified ? 1 : 0);
    ++i;
    sourceMint.write(_data, i);
    i += 32;
    sourceTokenAccount.write(_data, i);
    i += 32;
    providerProgram.write(_data, i);
    i += 32;
    providerConfig.write(_data, i);
    i += 32;
    putInt64LE(_data, i, sourceAmount);
    i += 8;
    putInt64LE(_data, i, quotedOutAmount);
    i += 8;
    putInt16LE(_data, i, destinationChain);
    i += 2;
    destinationRecipient.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(providerTransferId, 32, _data, i);
    providerEmitter.write(_data, i);
    i += 32;
    putInt64LE(_data, i, providerSequence);
    i += 8;
    putInt64LE(_data, i, committedSlot);
    i += 8;
    putInt64LE(_data, i, settledSlot);
    i += 8;
    putInt64LE(_data, i, reconciledSlot);
    i += 8;
    _data[i] = (byte) failureReason;
    ++i;
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
