// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.loopscale.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

/// @param principalAllowlist Principal mints that may be lent into Loopscale strategies.
///                           An empty list denies all strategy creation, updates, deposits, and ledger sales.
/// @param collateralAllowlist Collateral asset identifiers that vault-owned strategies may accept.
///                            An empty list denies all strategy collateral assets.
/// @param marketPolicies Optional per-market risk limits for strategy creation, updates, deposits, and ledger sales.
///                       If a market is present here, advanced per-market limits are enforced.
///                       If absent, lending operations are governed by top-level allowlists,
///                       permission checks, and sell-ledger limits.
/// @param sellLedgerPolicy Risk limits for selling Loopscale loan ledgers.
public record LendingPolicy(PublicKey[] principalAllowlist,
                            PublicKey[] collateralAllowlist,
                            LendingMarketPolicy[] marketPolicies,
                            SellLedgerPolicy sellLedgerPolicy) implements SerDe {

  public static final int PRINCIPAL_ALLOWLIST_OFFSET = 0;

  public static LendingPolicy read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var principalAllowlist = SerDeUtil.readPublicKeyVector(4, _data, i);
    i += SerDeUtil.lenVector(4, principalAllowlist);
    final var collateralAllowlist = SerDeUtil.readPublicKeyVector(4, _data, i);
    i += SerDeUtil.lenVector(4, collateralAllowlist);
    final var marketPolicies = SerDeUtil.readVector(4, LendingMarketPolicy.class, LendingMarketPolicy::read, _data, i);
    i += SerDeUtil.lenVector(4, marketPolicies);
    final var sellLedgerPolicy = SellLedgerPolicy.read(_data, i);
    return new LendingPolicy(principalAllowlist,
                             collateralAllowlist,
                             marketPolicies,
                             sellLedgerPolicy);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeVector(4, principalAllowlist, _data, i);
    i += SerDeUtil.writeVector(4, collateralAllowlist, _data, i);
    i += SerDeUtil.writeVector(4, marketPolicies, _data, i);
    i += sellLedgerPolicy.write(_data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return SerDeUtil.lenVector(4, principalAllowlist) + SerDeUtil.lenVector(4, collateralAllowlist) + SerDeUtil.lenVector(4, marketPolicies) + sellLedgerPolicy.l();
  }
}
