// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.exponent.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;

import systems.glam.sdk.idl.programs.glam.staging.exponent.gen.types.ExponentPolicy;

import java.util.List;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class ExtExponentProgram {

  public static final Discriminator SET_EXPONENT_POLICY_DISCRIMINATOR = toDiscriminator(144, 218, 118, 248, 42, 205, 27, 217);

  /// Set the Exponent Core policy for a GLAM vault.
  ///
  public static List<AccountMeta> setExponentPolicyKeys(final PublicKey glamStateKey,
                                                        final PublicKey glamSignerKey,
                                                        final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWritableSigner(glamSignerKey),
      createRead(glamProtocolProgramKey)
    );
  }

  /// Set the Exponent Core policy for a GLAM vault.
  ///
  public static Instruction setExponentPolicy(final AccountMeta invokedExtExponentProgramMeta,
                                              final PublicKey glamStateKey,
                                              final PublicKey glamSignerKey,
                                              final PublicKey glamProtocolProgramKey,
                                              final ExponentPolicy policy) {
    final var keys = setExponentPolicyKeys(
      glamStateKey,
      glamSignerKey,
      glamProtocolProgramKey
    );
    return setExponentPolicy(invokedExtExponentProgramMeta, keys, policy);
  }

  /// Set the Exponent Core policy for a GLAM vault.
  ///
  public static Instruction setExponentPolicy(final AccountMeta invokedExtExponentProgramMeta,
                                              final List<AccountMeta> keys,
                                              final ExponentPolicy policy) {
    final byte[] _data = new byte[8 + policy.l()];
    int i = SET_EXPONENT_POLICY_DISCRIMINATOR.write(_data, 0);
    policy.write(_data, i);

    return Instruction.createInstruction(invokedExtExponentProgramMeta, keys, _data);
  }

  public record SetExponentPolicyIxData(Discriminator discriminator, ExponentPolicy policy) implements SerDe {  

    public static SetExponentPolicyIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int POLICY_OFFSET = 8;

    public static SetExponentPolicyIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var policy = ExponentPolicy.read(_data, i);
      return new SetExponentPolicyIxData(discriminator, policy);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += policy.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + policy.l();
    }
  }

  public static final Discriminator WRAPPER_BUY_PT_DISCRIMINATOR = toDiscriminator(6, 127, 6, 136, 226, 194, 250, 168);

  /// Buy Exponent PT from an allowlisted Exponent market using the
  /// wrapper-compatible path.
  /// 
  /// This mints up to `max_base_amount` into SY first, then trades PT with the
  /// minted SY amount as the spend limit. Any unspent SY remains in the GLAM
  /// vault's SY token account.
  /// 
  /// - Permission: `BuyPt`.
  /// - Policy: the market must be in `markets_allowlist`.
  ///
  public static List<AccountMeta> wrapperBuyPtKeys(final SolanaAccounts solanaAccounts,
                                                   final PublicKey glamStateKey,
                                                   final PublicKey glamVaultKey,
                                                   final PublicKey glamSignerKey,
                                                   final PublicKey integrationAuthorityKey,
                                                   final PublicKey cpiProgramKey,
                                                   final PublicKey glamProtocolProgramKey,
                                                   final PublicKey marketKey,
                                                   final PublicKey tokenSyTraderKey,
                                                   final PublicKey tokenPtTraderKey,
                                                   final PublicKey tokenSyEscrowKey,
                                                   final PublicKey tokenPtEscrowKey,
                                                   final PublicKey addressLookupTableKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey tokenFeeTreasurySyKey,
                                                   final PublicKey eventAuthorityKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey)
    );
  }

  /// Buy Exponent PT from an allowlisted Exponent market using the
  /// wrapper-compatible path.
  /// 
  /// This mints up to `max_base_amount` into SY first, then trades PT with the
  /// minted SY amount as the spend limit. Any unspent SY remains in the GLAM
  /// vault's SY token account.
  /// 
  /// - Permission: `BuyPt`.
  /// - Policy: the market must be in `markets_allowlist`.
  ///
  /// @param ptAmount: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyRemAccountsUntil: u8
  public static Instruction wrapperBuyPt(final AccountMeta invokedExtExponentProgramMeta,
                                         final SolanaAccounts solanaAccounts,
                                         final PublicKey glamStateKey,
                                         final PublicKey glamVaultKey,
                                         final PublicKey glamSignerKey,
                                         final PublicKey integrationAuthorityKey,
                                         final PublicKey cpiProgramKey,
                                         final PublicKey glamProtocolProgramKey,
                                         final PublicKey marketKey,
                                         final PublicKey tokenSyTraderKey,
                                         final PublicKey tokenPtTraderKey,
                                         final PublicKey tokenSyEscrowKey,
                                         final PublicKey tokenPtEscrowKey,
                                         final PublicKey addressLookupTableKey,
                                         final PublicKey tokenProgramKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey tokenFeeTreasurySyKey,
                                         final PublicKey eventAuthorityKey,
                                         final long ptAmount,
                                         final long maxBaseAmount,
                                         final int mintSyRemAccountsUntil) {
    final var keys = wrapperBuyPtKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      marketKey,
      tokenSyTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey
    );
    return wrapperBuyPt(
      invokedExtExponentProgramMeta,
      keys,
      ptAmount,
      maxBaseAmount,
      mintSyRemAccountsUntil
    );
  }

  /// Buy Exponent PT from an allowlisted Exponent market using the
  /// wrapper-compatible path.
  /// 
  /// This mints up to `max_base_amount` into SY first, then trades PT with the
  /// minted SY amount as the spend limit. Any unspent SY remains in the GLAM
  /// vault's SY token account.
  /// 
  /// - Permission: `BuyPt`.
  /// - Policy: the market must be in `markets_allowlist`.
  ///
  /// @param ptAmount: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyRemAccountsUntil: u8
  public static Instruction wrapperBuyPt(final AccountMeta invokedExtExponentProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long ptAmount,
                                         final long maxBaseAmount,
                                         final int mintSyRemAccountsUntil) {
    final byte[] _data = new byte[25];
    int i = WRAPPER_BUY_PT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, ptAmount);
    i += 8;
    putInt64LE(_data, i, maxBaseAmount);
    i += 8;
    _data[i] = (byte) mintSyRemAccountsUntil;

    return Instruction.createInstruction(invokedExtExponentProgramMeta, keys, _data);
  }

  /// @param ptAmount: u64
  /// @param maxBaseAmount: u64
  /// @param mintSyRemAccountsUntil: u8
  public record WrapperBuyPtIxData(Discriminator discriminator,
                                   long ptAmount,
                                   long maxBaseAmount,
                                   int mintSyRemAccountsUntil) implements SerDe {  

    public static WrapperBuyPtIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 25;

    public static final int PT_AMOUNT_OFFSET = 8;
    public static final int MAX_BASE_AMOUNT_OFFSET = 16;
    public static final int MINT_SY_REM_ACCOUNTS_UNTIL_OFFSET = 24;

    public static WrapperBuyPtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var ptAmount = getInt64LE(_data, i);
      i += 8;
      final var maxBaseAmount = getInt64LE(_data, i);
      i += 8;
      final var mintSyRemAccountsUntil = _data[i] & 0xFF;
      return new WrapperBuyPtIxData(discriminator, ptAmount, maxBaseAmount, mintSyRemAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, ptAmount);
      i += 8;
      putInt64LE(_data, i, maxBaseAmount);
      i += 8;
      _data[i] = (byte) mintSyRemAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_MERGE_DISCRIMINATOR = toDiscriminator(177, 36, 171, 125, 89, 198, 144, 219);

  /// Merge Exponent PT and YT into SY, then redeem the resulting SY into base
  /// asset using the wrapper-compatible path.
  /// 
  /// - Permission: `Merge`.
  /// - Policy: the vault must be in `vaults_allowlist`.
  ///
  public static List<AccountMeta> wrapperMergeKeys(final SolanaAccounts solanaAccounts,
                                                   final PublicKey glamStateKey,
                                                   final PublicKey glamVaultKey,
                                                   final PublicKey glamSignerKey,
                                                   final PublicKey integrationAuthorityKey,
                                                   final PublicKey cpiProgramKey,
                                                   final PublicKey glamProtocolProgramKey,
                                                   final PublicKey tokenSyMergerKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey escrowSyKey,
                                                   final PublicKey tokenYtMergerKey,
                                                   final PublicKey tokenPtMergerKey,
                                                   final PublicKey mintYtKey,
                                                   final PublicKey mintPtKey,
                                                   final PublicKey authorityKey,
                                                   final PublicKey vaultAddressLookupTableKey,
                                                   final PublicKey tokenProgramKey,
                                                   final PublicKey vaultRobotYieldPositionKey,
                                                   final PublicKey syProgramKey,
                                                   final PublicKey eventAuthorityKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createWrite(tokenSyMergerKey),
      createWrite(vaultKey),
      createWrite(escrowSyKey),
      createWrite(tokenYtMergerKey),
      createWrite(tokenPtMergerKey),
      createWrite(mintYtKey),
      createWrite(mintPtKey),
      createWrite(authorityKey),
      createRead(vaultAddressLookupTableKey),
      createRead(tokenProgramKey),
      createWrite(vaultRobotYieldPositionKey),
      createRead(syProgramKey),
      createRead(eventAuthorityKey)
    );
  }

  /// Merge Exponent PT and YT into SY, then redeem the resulting SY into base
  /// asset using the wrapper-compatible path.
  /// 
  /// - Permission: `Merge`.
  /// - Policy: the vault must be in `vaults_allowlist`.
  ///
  /// @param amountPy: u64
  /// @param redeemSyAccountsUntil: u8
  public static Instruction wrapperMerge(final AccountMeta invokedExtExponentProgramMeta,
                                         final SolanaAccounts solanaAccounts,
                                         final PublicKey glamStateKey,
                                         final PublicKey glamVaultKey,
                                         final PublicKey glamSignerKey,
                                         final PublicKey integrationAuthorityKey,
                                         final PublicKey cpiProgramKey,
                                         final PublicKey glamProtocolProgramKey,
                                         final PublicKey tokenSyMergerKey,
                                         final PublicKey vaultKey,
                                         final PublicKey escrowSyKey,
                                         final PublicKey tokenYtMergerKey,
                                         final PublicKey tokenPtMergerKey,
                                         final PublicKey mintYtKey,
                                         final PublicKey mintPtKey,
                                         final PublicKey authorityKey,
                                         final PublicKey vaultAddressLookupTableKey,
                                         final PublicKey tokenProgramKey,
                                         final PublicKey vaultRobotYieldPositionKey,
                                         final PublicKey syProgramKey,
                                         final PublicKey eventAuthorityKey,
                                         final long amountPy,
                                         final int redeemSyAccountsUntil) {
    final var keys = wrapperMergeKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      tokenSyMergerKey,
      vaultKey,
      escrowSyKey,
      tokenYtMergerKey,
      tokenPtMergerKey,
      mintYtKey,
      mintPtKey,
      authorityKey,
      vaultAddressLookupTableKey,
      tokenProgramKey,
      vaultRobotYieldPositionKey,
      syProgramKey,
      eventAuthorityKey
    );
    return wrapperMerge(invokedExtExponentProgramMeta, keys, amountPy, redeemSyAccountsUntil);
  }

  /// Merge Exponent PT and YT into SY, then redeem the resulting SY into base
  /// asset using the wrapper-compatible path.
  /// 
  /// - Permission: `Merge`.
  /// - Policy: the vault must be in `vaults_allowlist`.
  ///
  /// @param amountPy: u64
  /// @param redeemSyAccountsUntil: u8
  public static Instruction wrapperMerge(final AccountMeta invokedExtExponentProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long amountPy,
                                         final int redeemSyAccountsUntil) {
    final byte[] _data = new byte[17];
    int i = WRAPPER_MERGE_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountPy);
    i += 8;
    _data[i] = (byte) redeemSyAccountsUntil;

    return Instruction.createInstruction(invokedExtExponentProgramMeta, keys, _data);
  }

  /// @param amountPy: u64
  /// @param redeemSyAccountsUntil: u8
  public record WrapperMergeIxData(Discriminator discriminator, long amountPy, int redeemSyAccountsUntil) implements SerDe {  

    public static WrapperMergeIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 17;

    public static final int AMOUNT_PY_OFFSET = 8;
    public static final int REDEEM_SY_ACCOUNTS_UNTIL_OFFSET = 16;

    public static WrapperMergeIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var amountPy = getInt64LE(_data, i);
      i += 8;
      final var redeemSyAccountsUntil = _data[i] & 0xFF;
      return new WrapperMergeIxData(discriminator, amountPy, redeemSyAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountPy);
      i += 8;
      _data[i] = (byte) redeemSyAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator WRAPPER_SELL_PT_DISCRIMINATOR = toDiscriminator(127, 65, 108, 12, 72, 21, 50, 200);

  /// Sell Exponent PT into an allowlisted Exponent market using the
  /// wrapper-compatible path.
  /// 
  /// - Permission: `SellPt`.
  /// - Policy: the market must be in `markets_allowlist`.
  ///
  public static List<AccountMeta> wrapperSellPtKeys(final SolanaAccounts solanaAccounts,
                                                    final PublicKey glamStateKey,
                                                    final PublicKey glamVaultKey,
                                                    final PublicKey glamSignerKey,
                                                    final PublicKey integrationAuthorityKey,
                                                    final PublicKey cpiProgramKey,
                                                    final PublicKey glamProtocolProgramKey,
                                                    final PublicKey marketKey,
                                                    final PublicKey tokenSyTraderKey,
                                                    final PublicKey tokenPtTraderKey,
                                                    final PublicKey tokenSyEscrowKey,
                                                    final PublicKey tokenPtEscrowKey,
                                                    final PublicKey addressLookupTableKey,
                                                    final PublicKey tokenProgramKey,
                                                    final PublicKey syProgramKey,
                                                    final PublicKey tokenFeeTreasurySyKey,
                                                    final PublicKey eventAuthorityKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createWrite(marketKey),
      createWrite(tokenSyTraderKey),
      createWrite(tokenPtTraderKey),
      createWrite(tokenSyEscrowKey),
      createWrite(tokenPtEscrowKey),
      createRead(addressLookupTableKey),
      createRead(tokenProgramKey),
      createRead(syProgramKey),
      createWrite(tokenFeeTreasurySyKey),
      createRead(eventAuthorityKey)
    );
  }

  /// Sell Exponent PT into an allowlisted Exponent market using the
  /// wrapper-compatible path.
  /// 
  /// - Permission: `SellPt`.
  /// - Policy: the market must be in `markets_allowlist`.
  ///
  /// @param amountPt: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyRemAccountsUntil: u8
  public static Instruction wrapperSellPt(final AccountMeta invokedExtExponentProgramMeta,
                                          final SolanaAccounts solanaAccounts,
                                          final PublicKey glamStateKey,
                                          final PublicKey glamVaultKey,
                                          final PublicKey glamSignerKey,
                                          final PublicKey integrationAuthorityKey,
                                          final PublicKey cpiProgramKey,
                                          final PublicKey glamProtocolProgramKey,
                                          final PublicKey marketKey,
                                          final PublicKey tokenSyTraderKey,
                                          final PublicKey tokenPtTraderKey,
                                          final PublicKey tokenSyEscrowKey,
                                          final PublicKey tokenPtEscrowKey,
                                          final PublicKey addressLookupTableKey,
                                          final PublicKey tokenProgramKey,
                                          final PublicKey syProgramKey,
                                          final PublicKey tokenFeeTreasurySyKey,
                                          final PublicKey eventAuthorityKey,
                                          final long amountPt,
                                          final long minBaseAmount,
                                          final int redeemSyRemAccountsUntil) {
    final var keys = wrapperSellPtKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      marketKey,
      tokenSyTraderKey,
      tokenPtTraderKey,
      tokenSyEscrowKey,
      tokenPtEscrowKey,
      addressLookupTableKey,
      tokenProgramKey,
      syProgramKey,
      tokenFeeTreasurySyKey,
      eventAuthorityKey
    );
    return wrapperSellPt(
      invokedExtExponentProgramMeta,
      keys,
      amountPt,
      minBaseAmount,
      redeemSyRemAccountsUntil
    );
  }

  /// Sell Exponent PT into an allowlisted Exponent market using the
  /// wrapper-compatible path.
  /// 
  /// - Permission: `SellPt`.
  /// - Policy: the market must be in `markets_allowlist`.
  ///
  /// @param amountPt: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyRemAccountsUntil: u8
  public static Instruction wrapperSellPt(final AccountMeta invokedExtExponentProgramMeta,
                                          final List<AccountMeta> keys,
                                          final long amountPt,
                                          final long minBaseAmount,
                                          final int redeemSyRemAccountsUntil) {
    final byte[] _data = new byte[25];
    int i = WRAPPER_SELL_PT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amountPt);
    i += 8;
    putInt64LE(_data, i, minBaseAmount);
    i += 8;
    _data[i] = (byte) redeemSyRemAccountsUntil;

    return Instruction.createInstruction(invokedExtExponentProgramMeta, keys, _data);
  }

  /// @param amountPt: u64
  /// @param minBaseAmount: u64
  /// @param redeemSyRemAccountsUntil: u8
  public record WrapperSellPtIxData(Discriminator discriminator,
                                    long amountPt,
                                    long minBaseAmount,
                                    int redeemSyRemAccountsUntil) implements SerDe {  

    public static WrapperSellPtIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 25;

    public static final int AMOUNT_PT_OFFSET = 8;
    public static final int MIN_BASE_AMOUNT_OFFSET = 16;
    public static final int REDEEM_SY_REM_ACCOUNTS_UNTIL_OFFSET = 24;

    public static WrapperSellPtIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var amountPt = getInt64LE(_data, i);
      i += 8;
      final var minBaseAmount = getInt64LE(_data, i);
      i += 8;
      final var redeemSyRemAccountsUntil = _data[i] & 0xFF;
      return new WrapperSellPtIxData(discriminator, amountPt, minBaseAmount, redeemSyRemAccountsUntil);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amountPt);
      i += 8;
      putInt64LE(_data, i, minBaseAmount);
      i += 8;
      _data[i] = (byte) redeemSyRemAccountsUntil;
      ++i;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private ExtExponentProgram() {
  }
}
