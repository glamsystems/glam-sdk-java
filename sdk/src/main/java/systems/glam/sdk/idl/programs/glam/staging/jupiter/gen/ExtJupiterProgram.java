// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.jupiter.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

import systems.glam.sdk.idl.programs.glam.staging.jupiter.gen.types.BorrowPolicy;
import systems.glam.sdk.idl.programs.glam.staging.jupiter.gen.types.EarnPolicy;
import systems.glam.sdk.idl.programs.glam.staging.jupiter.gen.types.TransferType;

import java.math.BigInteger;

import java.util.List;

import static java.util.Objects.requireNonNullElse;

import static software.sava.core.accounts.meta.AccountMeta.createRead;
import static software.sava.core.accounts.meta.AccountMeta.createWritableSigner;
import static software.sava.core.accounts.meta.AccountMeta.createWrite;
import static software.sava.core.encoding.ByteUtil.getInt128LE;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt128LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class ExtJupiterProgram {

  public static final Discriminator BORROW_INIT_POSITION_DISCRIMINATOR = toDiscriminator(92, 209, 176, 14, 75, 243, 107, 230);

  /// Initialize a Jupiter Borrow position NFT owned by the GLAM vault.
  /// 
  /// - Permission: `BorrowPermissions::InitPosition`.
  /// - Policy: `vault_state` must be in `BorrowPolicy::vaults_allowlist`.
  ///
  public static List<AccountMeta> borrowInitPositionKeys(final SolanaAccounts solanaAccounts,
                                                         final PublicKey glamStateKey,
                                                         final PublicKey glamVaultKey,
                                                         final PublicKey glamSignerKey,
                                                         final PublicKey integrationAuthorityKey,
                                                         final PublicKey cpiProgramKey,
                                                         final PublicKey glamProtocolProgramKey,
                                                         final PublicKey vaultAdminKey,
                                                         final PublicKey vaultStateKey,
                                                         final PublicKey positionKey,
                                                         final PublicKey positionMintKey,
                                                         final PublicKey positionTokenAccountKey,
                                                         final PublicKey metadataAccountKey,
                                                         final PublicKey tokenProgramKey,
                                                         final PublicKey metadataProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(vaultAdminKey),
      createWrite(vaultStateKey),
      createWrite(positionKey),
      createWrite(positionMintKey),
      createWrite(positionTokenAccountKey),
      createWrite(metadataAccountKey),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.associatedTokenAccountProgram()),
      createRead(solanaAccounts.instructionsSysVar()),
      createRead(metadataProgramKey),
      createRead(solanaAccounts.rentSysVar())
    );
  }

  /// Initialize a Jupiter Borrow position NFT owned by the GLAM vault.
  /// 
  /// - Permission: `BorrowPermissions::InitPosition`.
  /// - Policy: `vault_state` must be in `BorrowPolicy::vaults_allowlist`.
  ///
  /// @param vaultId: u16
  /// @param nextPositionId: u32
  public static Instruction borrowInitPosition(final AccountMeta invokedExtJupiterProgramMeta,
                                               final SolanaAccounts solanaAccounts,
                                               final PublicKey glamStateKey,
                                               final PublicKey glamVaultKey,
                                               final PublicKey glamSignerKey,
                                               final PublicKey integrationAuthorityKey,
                                               final PublicKey cpiProgramKey,
                                               final PublicKey glamProtocolProgramKey,
                                               final PublicKey vaultAdminKey,
                                               final PublicKey vaultStateKey,
                                               final PublicKey positionKey,
                                               final PublicKey positionMintKey,
                                               final PublicKey positionTokenAccountKey,
                                               final PublicKey metadataAccountKey,
                                               final PublicKey tokenProgramKey,
                                               final PublicKey metadataProgramKey,
                                               final int vaultId,
                                               final long nextPositionId) {
    final var keys = borrowInitPositionKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      vaultAdminKey,
      vaultStateKey,
      positionKey,
      positionMintKey,
      positionTokenAccountKey,
      metadataAccountKey,
      tokenProgramKey,
      metadataProgramKey
    );
    return borrowInitPosition(invokedExtJupiterProgramMeta, keys, vaultId, nextPositionId);
  }

  /// Initialize a Jupiter Borrow position NFT owned by the GLAM vault.
  /// 
  /// - Permission: `BorrowPermissions::InitPosition`.
  /// - Policy: `vault_state` must be in `BorrowPolicy::vaults_allowlist`.
  ///
  /// @param vaultId: u16
  /// @param nextPositionId: u32
  public static Instruction borrowInitPosition(final AccountMeta invokedExtJupiterProgramMeta,
                                               final List<AccountMeta> keys,
                                               final int vaultId,
                                               final long nextPositionId) {
    final byte[] _data = new byte[14];
    int i = BORROW_INIT_POSITION_DISCRIMINATOR.write(_data, 0);
    putInt16LE(_data, i, vaultId);
    i += 2;
    putInt32LE(_data, i, (int) nextPositionId);

    return Instruction.createInstruction(invokedExtJupiterProgramMeta, keys, _data);
  }

  /// @param vaultId: u16
  /// @param nextPositionId: u32
  public record BorrowInitPositionIxData(Discriminator discriminator, int vaultId, long nextPositionId) implements SerDe {

    public static BorrowInitPositionIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 14;

    public static final int VAULT_ID_OFFSET = 8;
    public static final int NEXT_POSITION_ID_OFFSET = 10;

    public static BorrowInitPositionIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var vaultId = Short.toUnsignedInt(getInt16LE(_data, i));
      i += 2;
      final var nextPositionId = Integer.toUnsignedLong(getInt32LE(_data, i));
      return new BorrowInitPositionIxData(discriminator, vaultId, nextPositionId);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt16LE(_data, i, vaultId);
      i += 2;
      putInt32LE(_data, i, (int) nextPositionId);
      i += 4;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator BORROW_OPERATE_DISCRIMINATOR = toDiscriminator(46, 121, 58, 48, 13, 5, 117, 188);

  /// Operate a Jupiter Borrow position.
  /// 
  /// Required permissions are derived from signed deltas:
  /// positive collateral = deposit, negative collateral = withdraw,
  /// positive debt = borrow, negative debt = repay.
  ///
  public static List<AccountMeta> borrowOperateKeys(final AccountMeta invokedExtJupiterProgramMeta,
                                                    final SolanaAccounts solanaAccounts,
                                                    final PublicKey glamStateKey,
                                                    final PublicKey glamVaultKey,
                                                    final PublicKey glamSignerKey,
                                                    final PublicKey integrationAuthorityKey,
                                                    final PublicKey cpiProgramKey,
                                                    final PublicKey glamProtocolProgramKey,
                                                    final PublicKey signerSupplyTokenAccountKey,
                                                    final PublicKey signerBorrowTokenAccountKey,
                                                    final PublicKey recipientKey,
                                                    final PublicKey recipientBorrowTokenAccountKey,
                                                    final PublicKey recipientSupplyTokenAccountKey,
                                                    final PublicKey vaultConfigKey,
                                                    final PublicKey vaultStateKey,
                                                    final PublicKey supplyTokenKey,
                                                    final PublicKey borrowTokenKey,
                                                    final PublicKey oracleKey,
                                                    final PublicKey positionKey,
                                                    final PublicKey positionTokenAccountKey,
                                                    final PublicKey currentPositionTickKey,
                                                    final PublicKey finalPositionTickKey,
                                                    final PublicKey currentPositionTickIdKey,
                                                    final PublicKey finalPositionTickIdKey,
                                                    final PublicKey newBranchKey,
                                                    final PublicKey supplyTokenReservesLiquidityKey,
                                                    final PublicKey borrowTokenReservesLiquidityKey,
                                                    final PublicKey vaultSupplyPositionOnLiquidityKey,
                                                    final PublicKey vaultBorrowPositionOnLiquidityKey,
                                                    final PublicKey supplyRateModelKey,
                                                    final PublicKey borrowRateModelKey,
                                                    final PublicKey vaultSupplyTokenAccountKey,
                                                    final PublicKey vaultBorrowTokenAccountKey,
                                                    final PublicKey supplyTokenClaimAccountKey,
                                                    final PublicKey borrowTokenClaimAccountKey,
                                                    final PublicKey liquidityKey,
                                                    final PublicKey liquidityProgramKey,
                                                    final PublicKey oracleProgramKey,
                                                    final PublicKey supplyTokenProgramKey,
                                                    final PublicKey borrowTokenProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createWrite(signerSupplyTokenAccountKey),
      createWrite(signerBorrowTokenAccountKey),
      createRead(requireNonNullElse(recipientKey, invokedExtJupiterProgramMeta.publicKey())),
      createWrite(requireNonNullElse(recipientBorrowTokenAccountKey, invokedExtJupiterProgramMeta.publicKey())),
      createWrite(requireNonNullElse(recipientSupplyTokenAccountKey, invokedExtJupiterProgramMeta.publicKey())),
      createRead(vaultConfigKey),
      createWrite(vaultStateKey),
      createRead(supplyTokenKey),
      createRead(borrowTokenKey),
      createRead(oracleKey),
      createWrite(positionKey),
      createRead(positionTokenAccountKey),
      createWrite(currentPositionTickKey),
      createWrite(finalPositionTickKey),
      createRead(currentPositionTickIdKey),
      createWrite(finalPositionTickIdKey),
      createWrite(newBranchKey),
      createWrite(supplyTokenReservesLiquidityKey),
      createWrite(borrowTokenReservesLiquidityKey),
      createWrite(vaultSupplyPositionOnLiquidityKey),
      createWrite(vaultBorrowPositionOnLiquidityKey),
      createRead(supplyRateModelKey),
      createRead(borrowRateModelKey),
      createWrite(vaultSupplyTokenAccountKey),
      createWrite(vaultBorrowTokenAccountKey),
      createWrite(requireNonNullElse(supplyTokenClaimAccountKey, invokedExtJupiterProgramMeta.publicKey())),
      createWrite(requireNonNullElse(borrowTokenClaimAccountKey, invokedExtJupiterProgramMeta.publicKey())),
      createRead(liquidityKey),
      createRead(liquidityProgramKey),
      createRead(oracleProgramKey),
      createRead(supplyTokenProgramKey),
      createRead(borrowTokenProgramKey),
      createRead(solanaAccounts.associatedTokenAccountProgram())
    );
  }

  /// Operate a Jupiter Borrow position.
  /// 
  /// Required permissions are derived from signed deltas:
  /// positive collateral = deposit, negative collateral = withdraw,
  /// positive debt = borrow, negative debt = repay.
  ///
  public static Instruction borrowOperate(final AccountMeta invokedExtJupiterProgramMeta,
                                          final SolanaAccounts solanaAccounts,
                                          final PublicKey glamStateKey,
                                          final PublicKey glamVaultKey,
                                          final PublicKey glamSignerKey,
                                          final PublicKey integrationAuthorityKey,
                                          final PublicKey cpiProgramKey,
                                          final PublicKey glamProtocolProgramKey,
                                          final PublicKey signerSupplyTokenAccountKey,
                                          final PublicKey signerBorrowTokenAccountKey,
                                          final PublicKey recipientKey,
                                          final PublicKey recipientBorrowTokenAccountKey,
                                          final PublicKey recipientSupplyTokenAccountKey,
                                          final PublicKey vaultConfigKey,
                                          final PublicKey vaultStateKey,
                                          final PublicKey supplyTokenKey,
                                          final PublicKey borrowTokenKey,
                                          final PublicKey oracleKey,
                                          final PublicKey positionKey,
                                          final PublicKey positionTokenAccountKey,
                                          final PublicKey currentPositionTickKey,
                                          final PublicKey finalPositionTickKey,
                                          final PublicKey currentPositionTickIdKey,
                                          final PublicKey finalPositionTickIdKey,
                                          final PublicKey newBranchKey,
                                          final PublicKey supplyTokenReservesLiquidityKey,
                                          final PublicKey borrowTokenReservesLiquidityKey,
                                          final PublicKey vaultSupplyPositionOnLiquidityKey,
                                          final PublicKey vaultBorrowPositionOnLiquidityKey,
                                          final PublicKey supplyRateModelKey,
                                          final PublicKey borrowRateModelKey,
                                          final PublicKey vaultSupplyTokenAccountKey,
                                          final PublicKey vaultBorrowTokenAccountKey,
                                          final PublicKey supplyTokenClaimAccountKey,
                                          final PublicKey borrowTokenClaimAccountKey,
                                          final PublicKey liquidityKey,
                                          final PublicKey liquidityProgramKey,
                                          final PublicKey oracleProgramKey,
                                          final PublicKey supplyTokenProgramKey,
                                          final PublicKey borrowTokenProgramKey,
                                          final BigInteger newCol,
                                          final BigInteger newDebt,
                                          final TransferType transferType,
                                          final byte[] remainingAccountsIndices) {
    final var keys = borrowOperateKeys(
      invokedExtJupiterProgramMeta,
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      signerSupplyTokenAccountKey,
      signerBorrowTokenAccountKey,
      recipientKey,
      recipientBorrowTokenAccountKey,
      recipientSupplyTokenAccountKey,
      vaultConfigKey,
      vaultStateKey,
      supplyTokenKey,
      borrowTokenKey,
      oracleKey,
      positionKey,
      positionTokenAccountKey,
      currentPositionTickKey,
      finalPositionTickKey,
      currentPositionTickIdKey,
      finalPositionTickIdKey,
      newBranchKey,
      supplyTokenReservesLiquidityKey,
      borrowTokenReservesLiquidityKey,
      vaultSupplyPositionOnLiquidityKey,
      vaultBorrowPositionOnLiquidityKey,
      supplyRateModelKey,
      borrowRateModelKey,
      vaultSupplyTokenAccountKey,
      vaultBorrowTokenAccountKey,
      supplyTokenClaimAccountKey,
      borrowTokenClaimAccountKey,
      liquidityKey,
      liquidityProgramKey,
      oracleProgramKey,
      supplyTokenProgramKey,
      borrowTokenProgramKey
    );
    return borrowOperate(
      invokedExtJupiterProgramMeta,
      keys,
      newCol,
      newDebt,
      transferType,
      remainingAccountsIndices
    );
  }

  /// Operate a Jupiter Borrow position.
  /// 
  /// Required permissions are derived from signed deltas:
  /// positive collateral = deposit, negative collateral = withdraw,
  /// positive debt = borrow, negative debt = repay.
  ///
  public static Instruction borrowOperate(final AccountMeta invokedExtJupiterProgramMeta,
                                          final List<AccountMeta> keys,
                                          final BigInteger newCol,
                                          final BigInteger newDebt,
                                          final TransferType transferType,
                                          final byte[] remainingAccountsIndices) {
    final byte[] _data = new byte[
    40
    + (transferType == null ? 1 : (1 + transferType.l())) + SerDeUtil.lenVector(4, remainingAccountsIndices)
    ];
    int i = BORROW_OPERATE_DISCRIMINATOR.write(_data, 0);
    putInt128LE(_data, i, newCol);
    i += 16;
    putInt128LE(_data, i, newDebt);
    i += 16;
    i += SerDeUtil.writeOptional(1, transferType, _data, i);
    SerDeUtil.writeVector(4, remainingAccountsIndices, _data, i);

    return Instruction.createInstruction(invokedExtJupiterProgramMeta, keys, _data);
  }

  public record BorrowOperateIxData(Discriminator discriminator,
                                    BigInteger newCol,
                                    BigInteger newDebt,
                                    TransferType transferType,
                                    byte[] remainingAccountsIndices) implements SerDe {

    public static BorrowOperateIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int NEW_COL_OFFSET = 8;
    public static final int NEW_DEBT_OFFSET = 24;
    public static final int TRANSFER_TYPE_OFFSET = 41;

    public static BorrowOperateIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var newCol = getInt128LE(_data, i);
      i += 16;
      final var newDebt = getInt128LE(_data, i);
      i += 16;
      final TransferType transferType;
      if (SerDeUtil.isAbsent(1, _data, i)) {
        transferType = null;
        ++i;
      } else {
        ++i;
        transferType = TransferType.read(_data, i);
        i += transferType.l();
      }
      final var remainingAccountsIndices = SerDeUtil.readbyteVector(4, _data, i);
      return new BorrowOperateIxData(discriminator,
                                     newCol,
                                     newDebt,
                                     transferType,
                                     remainingAccountsIndices);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt128LE(_data, i, newCol);
      i += 16;
      putInt128LE(_data, i, newDebt);
      i += 16;
      i += SerDeUtil.writeOptional(1, transferType, _data, i);
      i += SerDeUtil.writeVector(4, remainingAccountsIndices, _data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + 16 + 16 + (transferType == null ? 1 : (1 + transferType.l())) + SerDeUtil.lenVector(4, remainingAccountsIndices);
    }
  }

  public static final Discriminator EARN_DEPOSIT_DISCRIMINATOR = toDiscriminator(81, 98, 113, 207, 82, 192, 187, 234);

  /// Deposit underlying tokens into Jupiter Earn and mint jlTokens to the GLAM vault.
  /// 
  /// - Permission: `EarnPermissions::Deposit`.
  /// - Policy: underlying `mint` must be in `EarnPolicy::mints_allowlist`.
  ///
  public static List<AccountMeta> earnDepositKeys(final SolanaAccounts solanaAccounts,
                                                  final PublicKey glamStateKey,
                                                  final PublicKey glamVaultKey,
                                                  final PublicKey glamSignerKey,
                                                  final PublicKey integrationAuthorityKey,
                                                  final PublicKey cpiProgramKey,
                                                  final PublicKey glamProtocolProgramKey,
                                                  final PublicKey depositorTokenAccountKey,
                                                  final PublicKey recipientTokenAccountKey,
                                                  final PublicKey mintKey,
                                                  final PublicKey lendingAdminKey,
                                                  final PublicKey lendingKey,
                                                  final PublicKey fTokenMintKey,
                                                  final PublicKey supplyTokenReservesLiquidityKey,
                                                  final PublicKey lendingSupplyPositionOnLiquidityKey,
                                                  final PublicKey rateModelKey,
                                                  final PublicKey vaultKey,
                                                  final PublicKey liquidityKey,
                                                  final PublicKey liquidityProgramKey,
                                                  final PublicKey rewardsRateModelKey,
                                                  final PublicKey tokenProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createWrite(depositorTokenAccountKey),
      createWrite(recipientTokenAccountKey),
      createRead(mintKey),
      createRead(lendingAdminKey),
      createWrite(lendingKey),
      createWrite(fTokenMintKey),
      createWrite(supplyTokenReservesLiquidityKey),
      createWrite(lendingSupplyPositionOnLiquidityKey),
      createRead(rateModelKey),
      createWrite(vaultKey),
      createWrite(liquidityKey),
      createWrite(liquidityProgramKey),
      createRead(rewardsRateModelKey),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.associatedTokenAccountProgram())
    );
  }

  /// Deposit underlying tokens into Jupiter Earn and mint jlTokens to the GLAM vault.
  /// 
  /// - Permission: `EarnPermissions::Deposit`.
  /// - Policy: underlying `mint` must be in `EarnPolicy::mints_allowlist`.
  ///
  /// @param assets: u64
  /// @param minAmountOut: u64
  public static Instruction earnDeposit(final AccountMeta invokedExtJupiterProgramMeta,
                                        final SolanaAccounts solanaAccounts,
                                        final PublicKey glamStateKey,
                                        final PublicKey glamVaultKey,
                                        final PublicKey glamSignerKey,
                                        final PublicKey integrationAuthorityKey,
                                        final PublicKey cpiProgramKey,
                                        final PublicKey glamProtocolProgramKey,
                                        final PublicKey depositorTokenAccountKey,
                                        final PublicKey recipientTokenAccountKey,
                                        final PublicKey mintKey,
                                        final PublicKey lendingAdminKey,
                                        final PublicKey lendingKey,
                                        final PublicKey fTokenMintKey,
                                        final PublicKey supplyTokenReservesLiquidityKey,
                                        final PublicKey lendingSupplyPositionOnLiquidityKey,
                                        final PublicKey rateModelKey,
                                        final PublicKey vaultKey,
                                        final PublicKey liquidityKey,
                                        final PublicKey liquidityProgramKey,
                                        final PublicKey rewardsRateModelKey,
                                        final PublicKey tokenProgramKey,
                                        final long assets,
                                        final long minAmountOut) {
    final var keys = earnDepositKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      depositorTokenAccountKey,
      recipientTokenAccountKey,
      mintKey,
      lendingAdminKey,
      lendingKey,
      fTokenMintKey,
      supplyTokenReservesLiquidityKey,
      lendingSupplyPositionOnLiquidityKey,
      rateModelKey,
      vaultKey,
      liquidityKey,
      liquidityProgramKey,
      rewardsRateModelKey,
      tokenProgramKey
    );
    return earnDeposit(invokedExtJupiterProgramMeta, keys, assets, minAmountOut);
  }

  /// Deposit underlying tokens into Jupiter Earn and mint jlTokens to the GLAM vault.
  /// 
  /// - Permission: `EarnPermissions::Deposit`.
  /// - Policy: underlying `mint` must be in `EarnPolicy::mints_allowlist`.
  ///
  /// @param assets: u64
  /// @param minAmountOut: u64
  public static Instruction earnDeposit(final AccountMeta invokedExtJupiterProgramMeta,
                                        final List<AccountMeta> keys,
                                        final long assets,
                                        final long minAmountOut) {
    final byte[] _data = new byte[24];
    int i = EARN_DEPOSIT_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, assets);
    i += 8;
    putInt64LE(_data, i, minAmountOut);

    return Instruction.createInstruction(invokedExtJupiterProgramMeta, keys, _data);
  }

  /// @param assets: u64
  /// @param minAmountOut: u64
  public record EarnDepositIxData(Discriminator discriminator, long assets, long minAmountOut) implements SerDe {

    public static EarnDepositIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 24;

    public static final int ASSETS_OFFSET = 8;
    public static final int MIN_AMOUNT_OUT_OFFSET = 16;

    public static EarnDepositIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var assets = getInt64LE(_data, i);
      i += 8;
      final var minAmountOut = getInt64LE(_data, i);
      return new EarnDepositIxData(discriminator, assets, minAmountOut);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, assets);
      i += 8;
      putInt64LE(_data, i, minAmountOut);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator EARN_WITHDRAW_DISCRIMINATOR = toDiscriminator(68, 169, 40, 28, 165, 60, 157, 98);

  /// Burn Jupiter Earn jlTokens and withdraw underlying tokens to the GLAM vault.
  /// 
  /// - Permission: `EarnPermissions::Withdraw`.
  /// - Policy: underlying `mint` must be in `EarnPolicy::mints_allowlist`.
  ///
  public static List<AccountMeta> earnWithdrawKeys(final SolanaAccounts solanaAccounts,
                                                   final PublicKey glamStateKey,
                                                   final PublicKey glamVaultKey,
                                                   final PublicKey glamSignerKey,
                                                   final PublicKey integrationAuthorityKey,
                                                   final PublicKey cpiProgramKey,
                                                   final PublicKey glamProtocolProgramKey,
                                                   final PublicKey ownerTokenAccountKey,
                                                   final PublicKey recipientTokenAccountKey,
                                                   final PublicKey lendingAdminKey,
                                                   final PublicKey lendingKey,
                                                   final PublicKey mintKey,
                                                   final PublicKey fTokenMintKey,
                                                   final PublicKey supplyTokenReservesLiquidityKey,
                                                   final PublicKey lendingSupplyPositionOnLiquidityKey,
                                                   final PublicKey rateModelKey,
                                                   final PublicKey vaultKey,
                                                   final PublicKey claimAccountKey,
                                                   final PublicKey liquidityKey,
                                                   final PublicKey liquidityProgramKey,
                                                   final PublicKey rewardsRateModelKey,
                                                   final PublicKey tokenProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createWrite(ownerTokenAccountKey),
      createWrite(recipientTokenAccountKey),
      createRead(lendingAdminKey),
      createWrite(lendingKey),
      createRead(mintKey),
      createWrite(fTokenMintKey),
      createWrite(supplyTokenReservesLiquidityKey),
      createWrite(lendingSupplyPositionOnLiquidityKey),
      createRead(rateModelKey),
      createWrite(vaultKey),
      createWrite(claimAccountKey),
      createWrite(liquidityKey),
      createWrite(liquidityProgramKey),
      createRead(rewardsRateModelKey),
      createRead(tokenProgramKey),
      createRead(solanaAccounts.associatedTokenAccountProgram())
    );
  }

  /// Burn Jupiter Earn jlTokens and withdraw underlying tokens to the GLAM vault.
  /// 
  /// - Permission: `EarnPermissions::Withdraw`.
  /// - Policy: underlying `mint` must be in `EarnPolicy::mints_allowlist`.
  ///
  /// @param amount: u64
  /// @param maxSharesBurn: u64
  public static Instruction earnWithdraw(final AccountMeta invokedExtJupiterProgramMeta,
                                         final SolanaAccounts solanaAccounts,
                                         final PublicKey glamStateKey,
                                         final PublicKey glamVaultKey,
                                         final PublicKey glamSignerKey,
                                         final PublicKey integrationAuthorityKey,
                                         final PublicKey cpiProgramKey,
                                         final PublicKey glamProtocolProgramKey,
                                         final PublicKey ownerTokenAccountKey,
                                         final PublicKey recipientTokenAccountKey,
                                         final PublicKey lendingAdminKey,
                                         final PublicKey lendingKey,
                                         final PublicKey mintKey,
                                         final PublicKey fTokenMintKey,
                                         final PublicKey supplyTokenReservesLiquidityKey,
                                         final PublicKey lendingSupplyPositionOnLiquidityKey,
                                         final PublicKey rateModelKey,
                                         final PublicKey vaultKey,
                                         final PublicKey claimAccountKey,
                                         final PublicKey liquidityKey,
                                         final PublicKey liquidityProgramKey,
                                         final PublicKey rewardsRateModelKey,
                                         final PublicKey tokenProgramKey,
                                         final long amount,
                                         final long maxSharesBurn) {
    final var keys = earnWithdrawKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      ownerTokenAccountKey,
      recipientTokenAccountKey,
      lendingAdminKey,
      lendingKey,
      mintKey,
      fTokenMintKey,
      supplyTokenReservesLiquidityKey,
      lendingSupplyPositionOnLiquidityKey,
      rateModelKey,
      vaultKey,
      claimAccountKey,
      liquidityKey,
      liquidityProgramKey,
      rewardsRateModelKey,
      tokenProgramKey
    );
    return earnWithdraw(invokedExtJupiterProgramMeta, keys, amount, maxSharesBurn);
  }

  /// Burn Jupiter Earn jlTokens and withdraw underlying tokens to the GLAM vault.
  /// 
  /// - Permission: `EarnPermissions::Withdraw`.
  /// - Policy: underlying `mint` must be in `EarnPolicy::mints_allowlist`.
  ///
  /// @param amount: u64
  /// @param maxSharesBurn: u64
  public static Instruction earnWithdraw(final AccountMeta invokedExtJupiterProgramMeta,
                                         final List<AccountMeta> keys,
                                         final long amount,
                                         final long maxSharesBurn) {
    final byte[] _data = new byte[24];
    int i = EARN_WITHDRAW_DISCRIMINATOR.write(_data, 0);
    putInt64LE(_data, i, amount);
    i += 8;
    putInt64LE(_data, i, maxSharesBurn);

    return Instruction.createInstruction(invokedExtJupiterProgramMeta, keys, _data);
  }

  /// @param amount: u64
  /// @param maxSharesBurn: u64
  public record EarnWithdrawIxData(Discriminator discriminator, long amount, long maxSharesBurn) implements SerDe {

    public static EarnWithdrawIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 24;

    public static final int AMOUNT_OFFSET = 8;
    public static final int MAX_SHARES_BURN_OFFSET = 16;

    public static EarnWithdrawIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var amount = getInt64LE(_data, i);
      i += 8;
      final var maxSharesBurn = getInt64LE(_data, i);
      return new EarnWithdrawIxData(discriminator, amount, maxSharesBurn);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      putInt64LE(_data, i, amount);
      i += 8;
      putInt64LE(_data, i, maxSharesBurn);
      i += 8;
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_BORROW_POLICY_DISCRIMINATOR = toDiscriminator(199, 94, 106, 205, 150, 227, 206, 68);

  /// Set the Jupiter Borrow policy on the GLAM state.
  ///
  public static List<AccountMeta> setBorrowPolicyKeys(final PublicKey glamStateKey,
                                                      final PublicKey glamSignerKey,
                                                      final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWritableSigner(glamSignerKey),
      createRead(glamProtocolProgramKey)
    );
  }

  /// Set the Jupiter Borrow policy on the GLAM state.
  ///
  public static Instruction setBorrowPolicy(final AccountMeta invokedExtJupiterProgramMeta,
                                            final PublicKey glamStateKey,
                                            final PublicKey glamSignerKey,
                                            final PublicKey glamProtocolProgramKey,
                                            final BorrowPolicy policy) {
    final var keys = setBorrowPolicyKeys(
      glamStateKey,
      glamSignerKey,
      glamProtocolProgramKey
    );
    return setBorrowPolicy(invokedExtJupiterProgramMeta, keys, policy);
  }

  /// Set the Jupiter Borrow policy on the GLAM state.
  ///
  public static Instruction setBorrowPolicy(final AccountMeta invokedExtJupiterProgramMeta,
                                            final List<AccountMeta> keys,
                                            final BorrowPolicy policy) {
    final byte[] _data = new byte[8 + policy.l()];
    int i = SET_BORROW_POLICY_DISCRIMINATOR.write(_data, 0);
    policy.write(_data, i);

    return Instruction.createInstruction(invokedExtJupiterProgramMeta, keys, _data);
  }

  public record SetBorrowPolicyIxData(Discriminator discriminator, BorrowPolicy policy) implements SerDe {

    public static SetBorrowPolicyIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int POLICY_OFFSET = 8;

    public static SetBorrowPolicyIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var policy = BorrowPolicy.read(_data, i);
      return new SetBorrowPolicyIxData(discriminator, policy);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += policy.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + policy.l();
    }
  }

  public static final Discriminator SET_EARN_POLICY_DISCRIMINATOR = toDiscriminator(48, 17, 31, 62, 132, 232, 114, 96);

  /// Set the Jupiter Earn policy on the GLAM state.
  ///
  public static List<AccountMeta> setEarnPolicyKeys(final PublicKey glamStateKey,
                                                    final PublicKey glamSignerKey,
                                                    final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWritableSigner(glamSignerKey),
      createRead(glamProtocolProgramKey)
    );
  }

  /// Set the Jupiter Earn policy on the GLAM state.
  ///
  public static Instruction setEarnPolicy(final AccountMeta invokedExtJupiterProgramMeta,
                                          final PublicKey glamStateKey,
                                          final PublicKey glamSignerKey,
                                          final PublicKey glamProtocolProgramKey,
                                          final EarnPolicy policy) {
    final var keys = setEarnPolicyKeys(
      glamStateKey,
      glamSignerKey,
      glamProtocolProgramKey
    );
    return setEarnPolicy(invokedExtJupiterProgramMeta, keys, policy);
  }

  /// Set the Jupiter Earn policy on the GLAM state.
  ///
  public static Instruction setEarnPolicy(final AccountMeta invokedExtJupiterProgramMeta,
                                          final List<AccountMeta> keys,
                                          final EarnPolicy policy) {
    final byte[] _data = new byte[8 + policy.l()];
    int i = SET_EARN_POLICY_DISCRIMINATOR.write(_data, 0);
    policy.write(_data, i);

    return Instruction.createInstruction(invokedExtJupiterProgramMeta, keys, _data);
  }

  public record SetEarnPolicyIxData(Discriminator discriminator, EarnPolicy policy) implements SerDe {

    public static SetEarnPolicyIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int POLICY_OFFSET = 8;

    public static SetEarnPolicyIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var policy = EarnPolicy.read(_data, i);
      return new SetEarnPolicyIxData(discriminator, policy);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += policy.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + policy.l();
    }
  }

  private ExtJupiterProgram() {
  }
}
