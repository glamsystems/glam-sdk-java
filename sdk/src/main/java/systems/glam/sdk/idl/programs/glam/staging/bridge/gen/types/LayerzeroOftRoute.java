// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.bridge.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;

/// source_mint, destination_chain, destination_recipient, provider_program uniquely identify a route
///
/// @param destinationChain: u32
/// @param minAmount: u64
/// @param maxAmount: u64
public record LayerzeroOftRoute(PublicKey sourceMint,
                                long destinationChain,
                                PublicKey destinationRecipient,
                                PublicKey providerProgram,
                                RouteManagementMode managementMode,
                                long minAmount,
                                long maxAmount) implements SerDe {

  public static final int BYTES = 117;

  public static final int SOURCE_MINT_OFFSET = 0;
  public static final int DESTINATION_CHAIN_OFFSET = 32;
  public static final int DESTINATION_RECIPIENT_OFFSET = 36;
  public static final int PROVIDER_PROGRAM_OFFSET = 68;
  public static final int MANAGEMENT_MODE_OFFSET = 100;
  public static final int MIN_AMOUNT_OFFSET = 101;
  public static final int MAX_AMOUNT_OFFSET = 109;

  public static LayerzeroOftRoute read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var sourceMint = readPubKey(_data, i);
    i += 32;
    final var destinationChain = Integer.toUnsignedLong(getInt32LE(_data, i));
    i += 4;
    final var destinationRecipient = readPubKey(_data, i);
    i += 32;
    final var providerProgram = readPubKey(_data, i);
    i += 32;
    final var managementMode = RouteManagementMode.read(_data, i);
    i += managementMode.l();
    final var minAmount = getInt64LE(_data, i);
    i += 8;
    final var maxAmount = getInt64LE(_data, i);
    return new LayerzeroOftRoute(sourceMint,
                                 destinationChain,
                                 destinationRecipient,
                                 providerProgram,
                                 managementMode,
                                 minAmount,
                                 maxAmount);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    sourceMint.write(_data, i);
    i += 32;
    putInt32LE(_data, i, (int) destinationChain);
    i += 4;
    destinationRecipient.write(_data, i);
    i += 32;
    providerProgram.write(_data, i);
    i += 32;
    i += managementMode.write(_data, i);
    putInt64LE(_data, i, minAmount);
    i += 8;
    putInt64LE(_data, i, maxAmount);
    i += 8;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
