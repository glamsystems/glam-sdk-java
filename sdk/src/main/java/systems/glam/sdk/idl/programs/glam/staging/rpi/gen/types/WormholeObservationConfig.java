// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.rpi.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.core.programs.Discriminator;
import software.sava.core.rpc.Filter;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;
import software.sava.rpc.json.http.response.AccountInfo;

import java.util.function.BiFunction;

import static software.sava.core.accounts.PublicKey.readPubKey;
import static software.sava.core.encoding.ByteUtil.getInt16LE;
import static software.sava.core.encoding.ByteUtil.getInt32LE;
import static software.sava.core.encoding.ByteUtil.getInt64LE;
import static software.sava.core.encoding.ByteUtil.putInt16LE;
import static software.sava.core.encoding.ByteUtil.putInt32LE;
import static software.sava.core.encoding.ByteUtil.putInt64LE;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public record WormholeObservationConfig(PublicKey _address,
                                        Discriminator discriminator,
                                        PublicKey glamState,
                                        byte[] positionId,
                                        int emitterChain,
                                        byte[] emitterAddress,
                                        int payloadVersion,
                                        int payloadType,
                                        int maxAgeSeconds,
                                        boolean hasLastSequence,
                                        long lastSequence,
                                        byte[] lastVaaHash,
                                        int bump) implements SerDe {

  public static final int BYTES = 154;
  public static final int POSITION_ID_LEN = 32;
  public static final int EMITTER_ADDRESS_LEN = 32;
  public static final int LAST_VAA_HASH_LEN = 32;
  public static final Filter SIZE_FILTER = Filter.createDataSizeFilter(BYTES);

  public static final Discriminator DISCRIMINATOR = toDiscriminator(64, 123, 229, 21, 82, 252, 177, 166);
  public static final Filter DISCRIMINATOR_FILTER = Filter.createMemCompFilter(0, DISCRIMINATOR.data());

  public static final int GLAM_STATE_OFFSET = 8;
  public static final int POSITION_ID_OFFSET = 40;
  public static final int EMITTER_CHAIN_OFFSET = 72;
  public static final int EMITTER_ADDRESS_OFFSET = 74;
  public static final int PAYLOAD_VERSION_OFFSET = 106;
  public static final int PAYLOAD_TYPE_OFFSET = 107;
  public static final int MAX_AGE_SECONDS_OFFSET = 108;
  public static final int HAS_LAST_SEQUENCE_OFFSET = 112;
  public static final int LAST_SEQUENCE_OFFSET = 113;
  public static final int LAST_VAA_HASH_OFFSET = 121;
  public static final int BUMP_OFFSET = 153;

  public static Filter createGlamStateFilter(final PublicKey glamState) {
    return Filter.createMemCompFilter(GLAM_STATE_OFFSET, glamState);
  }

  public static Filter createEmitterChainFilter(final int emitterChain) {
    final byte[] _data = new byte[2];
    putInt16LE(_data, 0, emitterChain);
    return Filter.createMemCompFilter(EMITTER_CHAIN_OFFSET, _data);
  }

  public static Filter createPayloadVersionFilter(final int payloadVersion) {
    return Filter.createMemCompFilter(PAYLOAD_VERSION_OFFSET, new byte[]{(byte) payloadVersion});
  }

  public static Filter createPayloadTypeFilter(final int payloadType) {
    return Filter.createMemCompFilter(PAYLOAD_TYPE_OFFSET, new byte[]{(byte) payloadType});
  }

  public static Filter createMaxAgeSecondsFilter(final int maxAgeSeconds) {
    final byte[] _data = new byte[4];
    putInt32LE(_data, 0, maxAgeSeconds);
    return Filter.createMemCompFilter(MAX_AGE_SECONDS_OFFSET, _data);
  }

  public static Filter createHasLastSequenceFilter(final boolean hasLastSequence) {
    return Filter.createMemCompFilter(HAS_LAST_SEQUENCE_OFFSET, new byte[]{(byte) (hasLastSequence ? 1 : 0)});
  }

  public static Filter createLastSequenceFilter(final long lastSequence) {
    final byte[] _data = new byte[8];
    putInt64LE(_data, 0, lastSequence);
    return Filter.createMemCompFilter(LAST_SEQUENCE_OFFSET, _data);
  }

  public static Filter createBumpFilter(final int bump) {
    return Filter.createMemCompFilter(BUMP_OFFSET, new byte[]{(byte) bump});
  }

  public static WormholeObservationConfig read(final byte[] _data, final int _offset) {
    return read(null, _data, _offset);
  }

  public static WormholeObservationConfig read(final AccountInfo<byte[]> accountInfo) {
    return read(accountInfo.pubKey(), accountInfo.data(), 0);
  }

  public static WormholeObservationConfig read(final PublicKey _address, final byte[] _data) {
    return read(_address, _data, 0);
  }

  public static final BiFunction<PublicKey, byte[], WormholeObservationConfig> FACTORY = WormholeObservationConfig::read;

  public static WormholeObservationConfig read(final PublicKey _address, final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    final var discriminator = createAnchorDiscriminator(_data, _offset);
    int i = _offset + discriminator.length();
    final var glamState = readPubKey(_data, i);
    i += 32;
    final var positionId = new byte[32];
    i += SerDeUtil.readArray(positionId, _data, i);
    final var emitterChain = getInt16LE(_data, i);
    i += 2;
    final var emitterAddress = new byte[32];
    i += SerDeUtil.readArray(emitterAddress, _data, i);
    final var payloadVersion = _data[i] & 0xFF;
    ++i;
    final var payloadType = _data[i] & 0xFF;
    ++i;
    final var maxAgeSeconds = getInt32LE(_data, i);
    i += 4;
    final var hasLastSequence = _data[i] == 1;
    ++i;
    final var lastSequence = getInt64LE(_data, i);
    i += 8;
    final var lastVaaHash = new byte[32];
    i += SerDeUtil.readArray(lastVaaHash, _data, i);
    final var bump = _data[i] & 0xFF;
    return new WormholeObservationConfig(_address,
                                         discriminator,
                                         glamState,
                                         positionId,
                                         emitterChain,
                                         emitterAddress,
                                         payloadVersion,
                                         payloadType,
                                         maxAgeSeconds,
                                         hasLastSequence,
                                         lastSequence,
                                         lastVaaHash,
                                         bump);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset + discriminator.write(_data, _offset);
    glamState.write(_data, i);
    i += 32;
    i += SerDeUtil.writeArrayChecked(positionId, 32, _data, i);
    putInt16LE(_data, i, emitterChain);
    i += 2;
    i += SerDeUtil.writeArrayChecked(emitterAddress, 32, _data, i);
    _data[i] = (byte) payloadVersion;
    ++i;
    _data[i] = (byte) payloadType;
    ++i;
    putInt32LE(_data, i, maxAgeSeconds);
    i += 4;
    _data[i] = (byte) (hasLastSequence ? 1 : 0);
    ++i;
    putInt64LE(_data, i, lastSequence);
    i += 8;
    i += SerDeUtil.writeArrayChecked(lastVaaHash, 32, _data, i);
    _data[i] = (byte) bump;
    ++i;
    return i - _offset;
  }

  @Override
  public int l() {
    return BYTES;
  }
}
