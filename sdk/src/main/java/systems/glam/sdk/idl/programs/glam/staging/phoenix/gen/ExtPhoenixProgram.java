// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.phoenix.gen;

import software.sava.core.accounts.PublicKey;
import software.sava.core.accounts.SolanaAccounts;
import software.sava.core.accounts.meta.AccountMeta;
import software.sava.core.programs.Discriminator;
import software.sava.core.tx.Instruction;
import software.sava.idl.clients.core.gen.SerDe;
import systems.glam.sdk.idl.programs.glam.staging.phoenix.gen.types.*;

import java.util.List;

import static software.sava.core.accounts.meta.AccountMeta.*;
import static software.sava.core.programs.Discriminator.createAnchorDiscriminator;
import static software.sava.core.programs.Discriminator.toDiscriminator;

public final class ExtPhoenixProgram {

  public static final Discriminator CANCEL_ALL_DISCRIMINATOR = toDiscriminator(98, 191, 75, 220, 115, 40, 71, 237);

  /// Cancels all of the trader's resting orders on the market identified by the orderbook account.
  ///
  public static List<AccountMeta> cancelAllKeys(final SolanaAccounts solanaAccounts,
                                                final PublicKey glamStateKey,
                                                final PublicKey glamVaultKey,
                                                final PublicKey glamSignerKey,
                                                final PublicKey integrationAuthorityKey,
                                                final PublicKey cpiProgramKey,
                                                final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Cancels all of the trader's resting orders on the market identified by the orderbook account.
  ///
  public static Instruction cancelAll(final AccountMeta invokedExtPhoenixProgramMeta,
                                      final SolanaAccounts solanaAccounts,
                                      final PublicKey glamStateKey,
                                      final PublicKey glamVaultKey,
                                      final PublicKey glamSignerKey,
                                      final PublicKey integrationAuthorityKey,
                                      final PublicKey cpiProgramKey,
                                      final PublicKey glamProtocolProgramKey) {
    final var keys = cancelAllKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return cancelAll(invokedExtPhoenixProgramMeta, keys);
  }

  /// Cancels all of the trader's resting orders on the market identified by the orderbook account.
  ///
  public static Instruction cancelAll(final AccountMeta invokedExtPhoenixProgramMeta,
                                      final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, CANCEL_ALL_DISCRIMINATOR);
  }

  public static final Discriminator CANCEL_ORDERS_BY_ID_DISCRIMINATOR = toDiscriminator(234, 204, 126, 94, 222, 22, 141, 24);

  /// Cancels specific resting orders by their order IDs.
  ///
  public static List<AccountMeta> cancelOrdersByIdKeys(final SolanaAccounts solanaAccounts,
                                                       final PublicKey glamStateKey,
                                                       final PublicKey glamVaultKey,
                                                       final PublicKey glamSignerKey,
                                                       final PublicKey integrationAuthorityKey,
                                                       final PublicKey cpiProgramKey,
                                                       final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Cancels specific resting orders by their order IDs.
  ///
  public static Instruction cancelOrdersById(final AccountMeta invokedExtPhoenixProgramMeta,
                                             final SolanaAccounts solanaAccounts,
                                             final PublicKey glamStateKey,
                                             final PublicKey glamVaultKey,
                                             final PublicKey glamSignerKey,
                                             final PublicKey integrationAuthorityKey,
                                             final PublicKey cpiProgramKey,
                                             final PublicKey glamProtocolProgramKey,
                                             final OrderIds orderIds) {
    final var keys = cancelOrdersByIdKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return cancelOrdersById(invokedExtPhoenixProgramMeta, keys, orderIds);
  }

  /// Cancels specific resting orders by their order IDs.
  ///
  public static Instruction cancelOrdersById(final AccountMeta invokedExtPhoenixProgramMeta,
                                             final List<AccountMeta> keys,
                                             final OrderIds orderIds) {
    final byte[] _data = new byte[8 + orderIds.l()];
    int i = CANCEL_ORDERS_BY_ID_DISCRIMINATOR.write(_data, 0);
    orderIds.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record CancelOrdersByIdIxData(Discriminator discriminator, OrderIds orderIds) implements SerDe {  

    public static CancelOrdersByIdIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int ORDER_IDS_OFFSET = 8;

    public static CancelOrdersByIdIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var orderIds = OrderIds.read(_data, i);
      return new CancelOrdersByIdIxData(discriminator, orderIds);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += orderIds.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + orderIds.l();
    }
  }

  public static final Discriminator CANCEL_UP_TO_DISCRIMINATOR = toDiscriminator(26, 209, 244, 253, 59, 175, 227, 54);

  /// Cancels orders on one side of the book up to an optional count and/or tick limit.
  ///
  public static List<AccountMeta> cancelUpToKeys(final SolanaAccounts solanaAccounts,
                                                 final PublicKey glamStateKey,
                                                 final PublicKey glamVaultKey,
                                                 final PublicKey glamSignerKey,
                                                 final PublicKey integrationAuthorityKey,
                                                 final PublicKey cpiProgramKey,
                                                 final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Cancels orders on one side of the book up to an optional count and/or tick limit.
  ///
  public static Instruction cancelUpTo(final AccountMeta invokedExtPhoenixProgramMeta,
                                       final SolanaAccounts solanaAccounts,
                                       final PublicKey glamStateKey,
                                       final PublicKey glamVaultKey,
                                       final PublicKey glamSignerKey,
                                       final PublicKey integrationAuthorityKey,
                                       final PublicKey cpiProgramKey,
                                       final PublicKey glamProtocolProgramKey,
                                       final CancelUpToInstruction args) {
    final var keys = cancelUpToKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return cancelUpTo(invokedExtPhoenixProgramMeta, keys, args);
  }

  /// Cancels orders on one side of the book up to an optional count and/or tick limit.
  ///
  public static Instruction cancelUpTo(final AccountMeta invokedExtPhoenixProgramMeta,
                                       final List<AccountMeta> keys,
                                       final CancelUpToInstruction args) {
    final byte[] _data = new byte[8 + args.l()];
    int i = CANCEL_UP_TO_DISCRIMINATOR.write(_data, 0);
    args.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record CancelUpToIxData(Discriminator discriminator, CancelUpToInstruction args) implements SerDe {  

    public static CancelUpToIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int ARGS_OFFSET = 8;

    public static CancelUpToIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var args = CancelUpToInstruction.read(_data, i);
      return new CancelUpToIxData(discriminator, args);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += args.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + args.l();
    }
  }

  public static final Discriminator DEPOSIT_DISCRIMINATOR = toDiscriminator(242, 35, 198, 137, 82, 225, 242, 182);

  /// Ember deposit: converts the vault's input mint into the Phoenix-side output mint.
  ///
  public static List<AccountMeta> depositKeys(final SolanaAccounts solanaAccounts,
                                              final PublicKey glamStateKey,
                                              final PublicKey glamVaultKey,
                                              final PublicKey glamSignerKey,
                                              final PublicKey integrationAuthorityKey,
                                              final PublicKey cpiProgramKey,
                                              final PublicKey glamProtocolProgramKey,
                                              final PublicKey emberStateKey,
                                              final PublicKey inputMintKey,
                                              final PublicKey outputMintKey,
                                              final PublicKey inputTokenAccountKey,
                                              final PublicKey outputTokenAccountKey,
                                              final PublicKey emberVaultKey,
                                              final PublicKey tokenProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(emberStateKey),
      createRead(inputMintKey),
      createWrite(outputMintKey),
      createWrite(inputTokenAccountKey),
      createWrite(outputTokenAccountKey),
      createWrite(emberVaultKey),
      createRead(tokenProgramKey)
    );
  }

  /// Ember deposit: converts the vault's input mint into the Phoenix-side output mint.
  ///
  public static Instruction deposit(final AccountMeta invokedExtPhoenixProgramMeta,
                                    final SolanaAccounts solanaAccounts,
                                    final PublicKey glamStateKey,
                                    final PublicKey glamVaultKey,
                                    final PublicKey glamSignerKey,
                                    final PublicKey integrationAuthorityKey,
                                    final PublicKey cpiProgramKey,
                                    final PublicKey glamProtocolProgramKey,
                                    final PublicKey emberStateKey,
                                    final PublicKey inputMintKey,
                                    final PublicKey outputMintKey,
                                    final PublicKey inputTokenAccountKey,
                                    final PublicKey outputTokenAccountKey,
                                    final PublicKey emberVaultKey,
                                    final PublicKey tokenProgramKey,
                                    final DepositParams depositParams) {
    final var keys = depositKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      emberStateKey,
      inputMintKey,
      outputMintKey,
      inputTokenAccountKey,
      outputTokenAccountKey,
      emberVaultKey,
      tokenProgramKey
    );
    return deposit(invokedExtPhoenixProgramMeta, keys, depositParams);
  }

  /// Ember deposit: converts the vault's input mint into the Phoenix-side output mint.
  ///
  public static Instruction deposit(final AccountMeta invokedExtPhoenixProgramMeta,
                                    final List<AccountMeta> keys,
                                    final DepositParams depositParams) {
    final byte[] _data = new byte[8 + depositParams.l()];
    int i = DEPOSIT_DISCRIMINATOR.write(_data, 0);
    depositParams.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record DepositIxData(Discriminator discriminator, DepositParams depositParams) implements SerDe {  

    public static DepositIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int DEPOSIT_PARAMS_OFFSET = 8;

    public static DepositIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var depositParams = DepositParams.read(_data, i);
      return new DepositIxData(discriminator, depositParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += depositParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator DEPOSIT_FUNDS_DISCRIMINATOR = toDiscriminator(202, 39, 52, 211, 53, 20, 250, 88);

  /// Deposits base or quote mint tokens from the vault's token account into the Phoenix trader account.
  ///
  public static List<AccountMeta> depositFundsKeys(final SolanaAccounts solanaAccounts,
                                                   final PublicKey glamStateKey,
                                                   final PublicKey glamVaultKey,
                                                   final PublicKey glamSignerKey,
                                                   final PublicKey integrationAuthorityKey,
                                                   final PublicKey cpiProgramKey,
                                                   final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Deposits base or quote mint tokens from the vault's token account into the Phoenix trader account.
  ///
  public static Instruction depositFunds(final AccountMeta invokedExtPhoenixProgramMeta,
                                         final SolanaAccounts solanaAccounts,
                                         final PublicKey glamStateKey,
                                         final PublicKey glamVaultKey,
                                         final PublicKey glamSignerKey,
                                         final PublicKey integrationAuthorityKey,
                                         final PublicKey cpiProgramKey,
                                         final PublicKey glamProtocolProgramKey,
                                         final DepositFundsInstruction params) {
    final var keys = depositFundsKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return depositFunds(invokedExtPhoenixProgramMeta, keys, params);
  }

  /// Deposits base or quote mint tokens from the vault's token account into the Phoenix trader account.
  ///
  public static Instruction depositFunds(final AccountMeta invokedExtPhoenixProgramMeta,
                                         final List<AccountMeta> keys,
                                         final DepositFundsInstruction params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = DEPOSIT_FUNDS_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record DepositFundsIxData(Discriminator discriminator, DepositFundsInstruction params) implements SerDe {  

    public static DepositFundsIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int PARAMS_OFFSET = 8;

    public static DepositFundsIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = DepositFundsInstruction.read(_data, i);
      return new DepositFundsIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator PLACE_LIMIT_ORDER_DISCRIMINATOR = toDiscriminator(108, 176, 33, 186, 146, 229, 1, 197);

  /// Places a limit order; the unfilled portion rests on the orderbook.
  ///
  public static List<AccountMeta> placeLimitOrderKeys(final SolanaAccounts solanaAccounts,
                                                      final PublicKey glamStateKey,
                                                      final PublicKey glamVaultKey,
                                                      final PublicKey glamSignerKey,
                                                      final PublicKey integrationAuthorityKey,
                                                      final PublicKey cpiProgramKey,
                                                      final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Places a limit order; the unfilled portion rests on the orderbook.
  ///
  public static Instruction placeLimitOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                            final SolanaAccounts solanaAccounts,
                                            final PublicKey glamStateKey,
                                            final PublicKey glamVaultKey,
                                            final PublicKey glamSignerKey,
                                            final PublicKey integrationAuthorityKey,
                                            final PublicKey cpiProgramKey,
                                            final PublicKey glamProtocolProgramKey,
                                            final OrderPacket packet) {
    final var keys = placeLimitOrderKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return placeLimitOrder(invokedExtPhoenixProgramMeta, keys, packet);
  }

  /// Places a limit order; the unfilled portion rests on the orderbook.
  ///
  public static Instruction placeLimitOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                            final List<AccountMeta> keys,
                                            final OrderPacket packet) {
    final byte[] _data = new byte[8 + packet.l()];
    int i = PLACE_LIMIT_ORDER_DISCRIMINATOR.write(_data, 0);
    packet.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record PlaceLimitOrderIxData(Discriminator discriminator, OrderPacket packet) implements SerDe {  

    public static PlaceLimitOrderIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int PACKET_OFFSET = 8;

    public static PlaceLimitOrderIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var packet = OrderPacket.read(_data, i);
      return new PlaceLimitOrderIxData(discriminator, packet);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += packet.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + packet.l();
    }
  }

  public static final Discriminator PLACE_MARKET_ORDER_DISCRIMINATOR = toDiscriminator(90, 118, 192, 252, 192, 99, 39, 145);

  /// Places an immediate-or-cancel market order; unfilled size is dropped rather than rested.
  ///
  public static List<AccountMeta> placeMarketOrderKeys(final SolanaAccounts solanaAccounts,
                                                       final PublicKey glamStateKey,
                                                       final PublicKey glamVaultKey,
                                                       final PublicKey glamSignerKey,
                                                       final PublicKey integrationAuthorityKey,
                                                       final PublicKey cpiProgramKey,
                                                       final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Places an immediate-or-cancel market order; unfilled size is dropped rather than rested.
  ///
  public static Instruction placeMarketOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                             final SolanaAccounts solanaAccounts,
                                             final PublicKey glamStateKey,
                                             final PublicKey glamVaultKey,
                                             final PublicKey glamSignerKey,
                                             final PublicKey integrationAuthorityKey,
                                             final PublicKey cpiProgramKey,
                                             final PublicKey glamProtocolProgramKey,
                                             final OrderPacket packet) {
    final var keys = placeMarketOrderKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return placeMarketOrder(invokedExtPhoenixProgramMeta, keys, packet);
  }

  /// Places an immediate-or-cancel market order; unfilled size is dropped rather than rested.
  ///
  public static Instruction placeMarketOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                             final List<AccountMeta> keys,
                                             final OrderPacket packet) {
    final byte[] _data = new byte[8 + packet.l()];
    int i = PLACE_MARKET_ORDER_DISCRIMINATOR.write(_data, 0);
    packet.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record PlaceMarketOrderIxData(Discriminator discriminator, OrderPacket packet) implements SerDe {  

    public static PlaceMarketOrderIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int PACKET_OFFSET = 8;

    public static PlaceMarketOrderIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var packet = OrderPacket.read(_data, i);
      return new PlaceMarketOrderIxData(discriminator, packet);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += packet.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + packet.l();
    }
  }

  public static final Discriminator PLACE_MULTI_LIMIT_ORDER_DISCRIMINATOR = toDiscriminator(236, 208, 221, 172, 141, 226, 129, 84);

  /// Places multiple limit orders (bids and/or asks) on a single market in one CPI.
  ///
  public static List<AccountMeta> placeMultiLimitOrderKeys(final SolanaAccounts solanaAccounts,
                                                           final PublicKey glamStateKey,
                                                           final PublicKey glamVaultKey,
                                                           final PublicKey glamSignerKey,
                                                           final PublicKey integrationAuthorityKey,
                                                           final PublicKey cpiProgramKey,
                                                           final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Places multiple limit orders (bids and/or asks) on a single market in one CPI.
  ///
  public static Instruction placeMultiLimitOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                                 final SolanaAccounts solanaAccounts,
                                                 final PublicKey glamStateKey,
                                                 final PublicKey glamVaultKey,
                                                 final PublicKey glamSignerKey,
                                                 final PublicKey integrationAuthorityKey,
                                                 final PublicKey cpiProgramKey,
                                                 final PublicKey glamProtocolProgramKey,
                                                 final MultipleOrderPacket packet) {
    final var keys = placeMultiLimitOrderKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return placeMultiLimitOrder(invokedExtPhoenixProgramMeta, keys, packet);
  }

  /// Places multiple limit orders (bids and/or asks) on a single market in one CPI.
  ///
  public static Instruction placeMultiLimitOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                                 final List<AccountMeta> keys,
                                                 final MultipleOrderPacket packet) {
    final byte[] _data = new byte[8 + packet.l()];
    int i = PLACE_MULTI_LIMIT_ORDER_DISCRIMINATOR.write(_data, 0);
    packet.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record PlaceMultiLimitOrderIxData(Discriminator discriminator, MultipleOrderPacket packet) implements SerDe {  

    public static PlaceMultiLimitOrderIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int PACKET_OFFSET = 8;

    public static PlaceMultiLimitOrderIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var packet = MultipleOrderPacket.read(_data, i);
      return new PlaceMultiLimitOrderIxData(discriminator, packet);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += packet.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + packet.l();
    }
  }

  public static final Discriminator PLACE_POST_ONLY_ORDER_DISCRIMINATOR = toDiscriminator(253, 171, 187, 207, 158, 181, 93, 176);

  /// Places a maker-only limit order; rejected if it would cross the book.
  ///
  public static List<AccountMeta> placePostOnlyOrderKeys(final SolanaAccounts solanaAccounts,
                                                         final PublicKey glamStateKey,
                                                         final PublicKey glamVaultKey,
                                                         final PublicKey glamSignerKey,
                                                         final PublicKey integrationAuthorityKey,
                                                         final PublicKey cpiProgramKey,
                                                         final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Places a maker-only limit order; rejected if it would cross the book.
  ///
  public static Instruction placePostOnlyOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                               final SolanaAccounts solanaAccounts,
                                               final PublicKey glamStateKey,
                                               final PublicKey glamVaultKey,
                                               final PublicKey glamSignerKey,
                                               final PublicKey integrationAuthorityKey,
                                               final PublicKey cpiProgramKey,
                                               final PublicKey glamProtocolProgramKey,
                                               final OrderPacket packet) {
    final var keys = placePostOnlyOrderKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return placePostOnlyOrder(invokedExtPhoenixProgramMeta, keys, packet);
  }

  /// Places a maker-only limit order; rejected if it would cross the book.
  ///
  public static Instruction placePostOnlyOrder(final AccountMeta invokedExtPhoenixProgramMeta,
                                               final List<AccountMeta> keys,
                                               final OrderPacket packet) {
    final byte[] _data = new byte[8 + packet.l()];
    int i = PLACE_POST_ONLY_ORDER_DISCRIMINATOR.write(_data, 0);
    packet.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record PlacePostOnlyOrderIxData(Discriminator discriminator, OrderPacket packet) implements SerDe {  

    public static PlacePostOnlyOrderIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int PACKET_OFFSET = 8;

    public static PlacePostOnlyOrderIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var packet = OrderPacket.read(_data, i);
      return new PlacePostOnlyOrderIxData(discriminator, packet);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += packet.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + packet.l();
    }
  }

  public static final Discriminator REGISTER_TRADER_DISCRIMINATOR = toDiscriminator(75, 243, 224, 167, 1, 5, 51, 32);

  /// Initializes a Phoenix trader account (parent or child subaccount) owned by the vault.
  ///
  public static List<AccountMeta> registerTraderKeys(final SolanaAccounts solanaAccounts,
                                                     final PublicKey glamStateKey,
                                                     final PublicKey glamVaultKey,
                                                     final PublicKey glamSignerKey,
                                                     final PublicKey integrationAuthorityKey,
                                                     final PublicKey cpiProgramKey,
                                                     final PublicKey glamProtocolProgramKey,
                                                     final PublicKey logAuthorityKey,
                                                     final PublicKey globalConfigKey,
                                                     final PublicKey traderAccountKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(logAuthorityKey),
      createRead(globalConfigKey),
      createWrite(traderAccountKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Initializes a Phoenix trader account (parent or child subaccount) owned by the vault.
  ///
  public static Instruction registerTrader(final AccountMeta invokedExtPhoenixProgramMeta,
                                           final SolanaAccounts solanaAccounts,
                                           final PublicKey glamStateKey,
                                           final PublicKey glamVaultKey,
                                           final PublicKey glamSignerKey,
                                           final PublicKey integrationAuthorityKey,
                                           final PublicKey cpiProgramKey,
                                           final PublicKey glamProtocolProgramKey,
                                           final PublicKey logAuthorityKey,
                                           final PublicKey globalConfigKey,
                                           final PublicKey traderAccountKey,
                                           final RegisterTraderParams args) {
    final var keys = registerTraderKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      logAuthorityKey,
      globalConfigKey,
      traderAccountKey
    );
    return registerTrader(invokedExtPhoenixProgramMeta, keys, args);
  }

  /// Initializes a Phoenix trader account (parent or child subaccount) owned by the vault.
  ///
  public static Instruction registerTrader(final AccountMeta invokedExtPhoenixProgramMeta,
                                           final List<AccountMeta> keys,
                                           final RegisterTraderParams args) {
    final byte[] _data = new byte[8 + args.l()];
    int i = REGISTER_TRADER_DISCRIMINATOR.write(_data, 0);
    args.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record RegisterTraderIxData(Discriminator discriminator, RegisterTraderParams args) implements SerDe {  

    public static RegisterTraderIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 18;

    public static final int ARGS_OFFSET = 8;

    public static RegisterTraderIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var args = RegisterTraderParams.read(_data, i);
      return new RegisterTraderIxData(discriminator, args);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += args.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator SET_PHOENIX_POLICY_DISCRIMINATOR = toDiscriminator(57, 235, 161, 196, 65, 41, 80, 85);

  /// Sets the GLAM Phoenix policy (market allowlist, order types, and price-deviation limits) on the vault state.
  ///
  public static List<AccountMeta> setPhoenixPolicyKeys(final PublicKey glamStateKey,
                                                       final PublicKey glamSignerKey,
                                                       final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWritableSigner(glamSignerKey),
      createRead(glamProtocolProgramKey)
    );
  }

  /// Sets the GLAM Phoenix policy (market allowlist, order types, and price-deviation limits) on the vault state.
  ///
  public static Instruction setPhoenixPolicy(final AccountMeta invokedExtPhoenixProgramMeta,
                                             final PublicKey glamStateKey,
                                             final PublicKey glamSignerKey,
                                             final PublicKey glamProtocolProgramKey,
                                             final PhoenixPolicy policy) {
    final var keys = setPhoenixPolicyKeys(
      glamStateKey,
      glamSignerKey,
      glamProtocolProgramKey
    );
    return setPhoenixPolicy(invokedExtPhoenixProgramMeta, keys, policy);
  }

  /// Sets the GLAM Phoenix policy (market allowlist, order types, and price-deviation limits) on the vault state.
  ///
  public static Instruction setPhoenixPolicy(final AccountMeta invokedExtPhoenixProgramMeta,
                                             final List<AccountMeta> keys,
                                             final PhoenixPolicy policy) {
    final byte[] _data = new byte[8 + policy.l()];
    int i = SET_PHOENIX_POLICY_DISCRIMINATOR.write(_data, 0);
    policy.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record SetPhoenixPolicyIxData(Discriminator discriminator, PhoenixPolicy policy) implements SerDe {  

    public static SetPhoenixPolicyIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int POLICY_OFFSET = 8;

    public static SetPhoenixPolicyIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var policy = PhoenixPolicy.read(_data, i);
      return new SetPhoenixPolicyIxData(discriminator, policy);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += policy.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + policy.l();
    }
  }

  public static final Discriminator SYNC_PARENT_TO_CHILD_DISCRIMINATOR = toDiscriminator(175, 137, 217, 11, 235, 39, 150, 19);

  /// Syncs collateral and state from a parent trader account down to a child subaccount.
  ///
  public static List<AccountMeta> syncParentToChildKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey glamStateKey,
                                                        final PublicKey glamVaultKey,
                                                        final PublicKey glamSignerKey,
                                                        final PublicKey integrationAuthorityKey,
                                                        final PublicKey cpiProgramKey,
                                                        final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Syncs collateral and state from a parent trader account down to a child subaccount.
  ///
  public static Instruction syncParentToChild(final AccountMeta invokedExtPhoenixProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey glamStateKey,
                                              final PublicKey glamVaultKey,
                                              final PublicKey glamSignerKey,
                                              final PublicKey integrationAuthorityKey,
                                              final PublicKey cpiProgramKey,
                                              final PublicKey glamProtocolProgramKey) {
    final var keys = syncParentToChildKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return syncParentToChild(invokedExtPhoenixProgramMeta, keys);
  }

  /// Syncs collateral and state from a parent trader account down to a child subaccount.
  ///
  public static Instruction syncParentToChild(final AccountMeta invokedExtPhoenixProgramMeta,
                                              final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, SYNC_PARENT_TO_CHILD_DISCRIMINATOR);
  }

  public static final Discriminator TRANSFER_COLLATERAL_DISCRIMINATOR = toDiscriminator(157, 163, 63, 27, 242, 72, 251, 97);

  /// Transfers collateral from a source trader subaccount to a destination trader subaccount.
  ///
  public static List<AccountMeta> transferCollateralKeys(final SolanaAccounts solanaAccounts,
                                                         final PublicKey glamStateKey,
                                                         final PublicKey glamVaultKey,
                                                         final PublicKey glamSignerKey,
                                                         final PublicKey integrationAuthorityKey,
                                                         final PublicKey cpiProgramKey,
                                                         final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Transfers collateral from a source trader subaccount to a destination trader subaccount.
  ///
  public static Instruction transferCollateral(final AccountMeta invokedExtPhoenixProgramMeta,
                                               final SolanaAccounts solanaAccounts,
                                               final PublicKey glamStateKey,
                                               final PublicKey glamVaultKey,
                                               final PublicKey glamSignerKey,
                                               final PublicKey integrationAuthorityKey,
                                               final PublicKey cpiProgramKey,
                                               final PublicKey glamProtocolProgramKey,
                                               final TransferCollateralInstruction params) {
    final var keys = transferCollateralKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return transferCollateral(invokedExtPhoenixProgramMeta, keys, params);
  }

  /// Transfers collateral from a source trader subaccount to a destination trader subaccount.
  ///
  public static Instruction transferCollateral(final AccountMeta invokedExtPhoenixProgramMeta,
                                               final List<AccountMeta> keys,
                                               final TransferCollateralInstruction params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = TRANSFER_COLLATERAL_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record TransferCollateralIxData(Discriminator discriminator, TransferCollateralInstruction params) implements SerDe {  

    public static TransferCollateralIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int PARAMS_OFFSET = 8;

    public static TransferCollateralIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = TransferCollateralInstruction.read(_data, i);
      return new TransferCollateralIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator TRANSFER_COLLATERAL_CHILD_TO_PARENT_DISCRIMINATOR = toDiscriminator(51, 100, 177, 115, 139, 135, 247, 139);

  /// Transfers collateral from a child subaccount back to its parent trader account.
  ///
  public static List<AccountMeta> transferCollateralChildToParentKeys(final SolanaAccounts solanaAccounts,
                                                                      final PublicKey glamStateKey,
                                                                      final PublicKey glamVaultKey,
                                                                      final PublicKey glamSignerKey,
                                                                      final PublicKey integrationAuthorityKey,
                                                                      final PublicKey cpiProgramKey,
                                                                      final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Transfers collateral from a child subaccount back to its parent trader account.
  ///
  public static Instruction transferCollateralChildToParent(final AccountMeta invokedExtPhoenixProgramMeta,
                                                            final SolanaAccounts solanaAccounts,
                                                            final PublicKey glamStateKey,
                                                            final PublicKey glamVaultKey,
                                                            final PublicKey glamSignerKey,
                                                            final PublicKey integrationAuthorityKey,
                                                            final PublicKey cpiProgramKey,
                                                            final PublicKey glamProtocolProgramKey,
                                                            final TransferCollateralChildToParentInstruction params) {
    final var keys = transferCollateralChildToParentKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return transferCollateralChildToParent(invokedExtPhoenixProgramMeta, keys, params);
  }

  /// Transfers collateral from a child subaccount back to its parent trader account.
  ///
  public static Instruction transferCollateralChildToParent(final AccountMeta invokedExtPhoenixProgramMeta,
                                                            final List<AccountMeta> keys,
                                                            final TransferCollateralChildToParentInstruction params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = TRANSFER_COLLATERAL_CHILD_TO_PARENT_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record TransferCollateralChildToParentIxData(Discriminator discriminator, TransferCollateralChildToParentInstruction params) implements SerDe {  

    public static TransferCollateralChildToParentIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int PARAMS_OFFSET = 8;

    public static TransferCollateralChildToParentIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = TransferCollateralChildToParentInstruction.read(_data, i);
      return new TransferCollateralChildToParentIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  public static final Discriminator UPDATE_TRADER_STATE_DISCRIMINATOR = toDiscriminator(249, 139, 82, 44, 126, 66, 133, 220);

  /// Refreshes the trader account's cached state (PnL, funding, margin) against current market data.
  ///
  public static List<AccountMeta> updateTraderStateKeys(final SolanaAccounts solanaAccounts,
                                                        final PublicKey glamStateKey,
                                                        final PublicKey glamVaultKey,
                                                        final PublicKey glamSignerKey,
                                                        final PublicKey integrationAuthorityKey,
                                                        final PublicKey cpiProgramKey,
                                                        final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Refreshes the trader account's cached state (PnL, funding, margin) against current market data.
  ///
  public static Instruction updateTraderState(final AccountMeta invokedExtPhoenixProgramMeta,
                                              final SolanaAccounts solanaAccounts,
                                              final PublicKey glamStateKey,
                                              final PublicKey glamVaultKey,
                                              final PublicKey glamSignerKey,
                                              final PublicKey integrationAuthorityKey,
                                              final PublicKey cpiProgramKey,
                                              final PublicKey glamProtocolProgramKey) {
    final var keys = updateTraderStateKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return updateTraderState(invokedExtPhoenixProgramMeta, keys);
  }

  /// Refreshes the trader account's cached state (PnL, funding, margin) against current market data.
  ///
  public static Instruction updateTraderState(final AccountMeta invokedExtPhoenixProgramMeta,
                                              final List<AccountMeta> keys) {
    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, UPDATE_TRADER_STATE_DISCRIMINATOR);
  }

  public static final Discriminator WITHDRAW_DISCRIMINATOR = toDiscriminator(183, 18, 70, 156, 148, 109, 161, 34);

  /// Ember withdraw: converts the Phoenix-side output mint back to the input mint; `None` = full balance.
  ///
  public static List<AccountMeta> withdrawKeys(final SolanaAccounts solanaAccounts,
                                               final PublicKey glamStateKey,
                                               final PublicKey glamVaultKey,
                                               final PublicKey glamSignerKey,
                                               final PublicKey integrationAuthorityKey,
                                               final PublicKey cpiProgramKey,
                                               final PublicKey glamProtocolProgramKey,
                                               final PublicKey emberStateKey,
                                               final PublicKey inputMintKey,
                                               final PublicKey outputMintKey,
                                               final PublicKey inputTokenAccountKey,
                                               final PublicKey outputTokenAccountKey,
                                               final PublicKey emberVaultKey,
                                               final PublicKey tokenProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram()),
      createRead(emberStateKey),
      createRead(inputMintKey),
      createWrite(outputMintKey),
      createWrite(inputTokenAccountKey),
      createWrite(outputTokenAccountKey),
      createWrite(emberVaultKey),
      createRead(tokenProgramKey)
    );
  }

  /// Ember withdraw: converts the Phoenix-side output mint back to the input mint; `None` = full balance.
  ///
  public static Instruction withdraw(final AccountMeta invokedExtPhoenixProgramMeta,
                                     final SolanaAccounts solanaAccounts,
                                     final PublicKey glamStateKey,
                                     final PublicKey glamVaultKey,
                                     final PublicKey glamSignerKey,
                                     final PublicKey integrationAuthorityKey,
                                     final PublicKey cpiProgramKey,
                                     final PublicKey glamProtocolProgramKey,
                                     final PublicKey emberStateKey,
                                     final PublicKey inputMintKey,
                                     final PublicKey outputMintKey,
                                     final PublicKey inputTokenAccountKey,
                                     final PublicKey outputTokenAccountKey,
                                     final PublicKey emberVaultKey,
                                     final PublicKey tokenProgramKey,
                                     final WithdrawParams withdrawParams) {
    final var keys = withdrawKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey,
      emberStateKey,
      inputMintKey,
      outputMintKey,
      inputTokenAccountKey,
      outputTokenAccountKey,
      emberVaultKey,
      tokenProgramKey
    );
    return withdraw(invokedExtPhoenixProgramMeta, keys, withdrawParams);
  }

  /// Ember withdraw: converts the Phoenix-side output mint back to the input mint; `None` = full balance.
  ///
  public static Instruction withdraw(final AccountMeta invokedExtPhoenixProgramMeta,
                                     final List<AccountMeta> keys,
                                     final WithdrawParams withdrawParams) {
    final byte[] _data = new byte[8 + withdrawParams.l()];
    int i = WITHDRAW_DISCRIMINATOR.write(_data, 0);
    withdrawParams.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record WithdrawIxData(Discriminator discriminator, WithdrawParams withdrawParams) implements SerDe {  

    public static WithdrawIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int WITHDRAW_PARAMS_OFFSET = 8;

    public static WithdrawIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var withdrawParams = WithdrawParams.read(_data, i);
      return new WithdrawIxData(discriminator, withdrawParams);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += withdrawParams.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return 8 + withdrawParams.l();
    }
  }

  public static final Discriminator WITHDRAW_FUNDS_DISCRIMINATOR = toDiscriminator(241, 36, 29, 111, 208, 31, 104, 217);

  /// Withdraws base or quote mint tokens from the Phoenix trader account back to the vault's token account.
  ///
  public static List<AccountMeta> withdrawFundsKeys(final SolanaAccounts solanaAccounts,
                                                    final PublicKey glamStateKey,
                                                    final PublicKey glamVaultKey,
                                                    final PublicKey glamSignerKey,
                                                    final PublicKey integrationAuthorityKey,
                                                    final PublicKey cpiProgramKey,
                                                    final PublicKey glamProtocolProgramKey) {
    return List.of(
      createWrite(glamStateKey),
      createWrite(glamVaultKey),
      createWritableSigner(glamSignerKey),
      createRead(integrationAuthorityKey),
      createRead(cpiProgramKey),
      createRead(glamProtocolProgramKey),
      createRead(solanaAccounts.systemProgram())
    );
  }

  /// Withdraws base or quote mint tokens from the Phoenix trader account back to the vault's token account.
  ///
  public static Instruction withdrawFunds(final AccountMeta invokedExtPhoenixProgramMeta,
                                          final SolanaAccounts solanaAccounts,
                                          final PublicKey glamStateKey,
                                          final PublicKey glamVaultKey,
                                          final PublicKey glamSignerKey,
                                          final PublicKey integrationAuthorityKey,
                                          final PublicKey cpiProgramKey,
                                          final PublicKey glamProtocolProgramKey,
                                          final WithdrawFundsInstruction params) {
    final var keys = withdrawFundsKeys(
      solanaAccounts,
      glamStateKey,
      glamVaultKey,
      glamSignerKey,
      integrationAuthorityKey,
      cpiProgramKey,
      glamProtocolProgramKey
    );
    return withdrawFunds(invokedExtPhoenixProgramMeta, keys, params);
  }

  /// Withdraws base or quote mint tokens from the Phoenix trader account back to the vault's token account.
  ///
  public static Instruction withdrawFunds(final AccountMeta invokedExtPhoenixProgramMeta,
                                          final List<AccountMeta> keys,
                                          final WithdrawFundsInstruction params) {
    final byte[] _data = new byte[8 + params.l()];
    int i = WITHDRAW_FUNDS_DISCRIMINATOR.write(_data, 0);
    params.write(_data, i);

    return Instruction.createInstruction(invokedExtPhoenixProgramMeta, keys, _data);
  }

  public record WithdrawFundsIxData(Discriminator discriminator, WithdrawFundsInstruction params) implements SerDe {  

    public static WithdrawFundsIxData read(final Instruction instruction) {
      return read(instruction.data(), instruction.offset());
    }

    public static final int BYTES = 16;

    public static final int PARAMS_OFFSET = 8;

    public static WithdrawFundsIxData read(final byte[] _data, final int _offset) {
      if (_data == null || _data.length == 0) {
        return null;
      }
      final var discriminator = createAnchorDiscriminator(_data, _offset);
      int i = _offset + discriminator.length();
      final var params = WithdrawFundsInstruction.read(_data, i);
      return new WithdrawFundsIxData(discriminator, params);
    }

    @Override
    public int write(final byte[] _data, final int _offset) {
      int i = _offset + discriminator.write(_data, _offset);
      i += params.write(_data, i);
      return i - _offset;
    }

    @Override
    public int l() {
      return BYTES;
    }
  }

  private ExtPhoenixProgram() {
  }
}
