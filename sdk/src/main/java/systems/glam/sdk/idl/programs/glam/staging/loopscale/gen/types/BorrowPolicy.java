// @generated by sava-software idl-src-gen — DO NOT EDIT
package systems.glam.sdk.idl.programs.glam.staging.loopscale.gen.types;

import software.sava.core.accounts.PublicKey;
import software.sava.idl.clients.core.gen.SerDe;
import software.sava.idl.clients.core.gen.SerDeUtil;

/// @param collateralAllowlist Collateral mints that may be deposited into Loopscale borrow loans.
/// @param principalAllowlist Principal mints that may be borrowed from Loopscale borrow markets.
///                           An empty list denies all principal borrows.
/// @param marketPolicies Optional per-market risk limits for borrow operations.
///                       If a market is present here, advanced per-market limits are enforced.
///                       If absent, borrow operations are governed by the top-level allowlists.
public record BorrowPolicy(PublicKey[] collateralAllowlist,
                           PublicKey[] principalAllowlist,
                           BorrowMarketPolicy[] marketPolicies) implements SerDe {

  public static final int COLLATERAL_ALLOWLIST_OFFSET = 0;

  public static BorrowPolicy read(final byte[] _data, final int _offset) {
    if (_data == null || _data.length == 0) {
      return null;
    }
    int i = _offset;
    final var collateralAllowlist = SerDeUtil.readPublicKeyVector(4, _data, i);
    i += SerDeUtil.lenVector(4, collateralAllowlist);
    final var principalAllowlist = SerDeUtil.readPublicKeyVector(4, _data, i);
    i += SerDeUtil.lenVector(4, principalAllowlist);
    final var marketPolicies = SerDeUtil.readVector(4, BorrowMarketPolicy.class, BorrowMarketPolicy::read, _data, i);
    return new BorrowPolicy(collateralAllowlist, principalAllowlist, marketPolicies);
  }

  @Override
  public int write(final byte[] _data, final int _offset) {
    int i = _offset;
    i += SerDeUtil.writeVector(4, collateralAllowlist, _data, i);
    i += SerDeUtil.writeVector(4, principalAllowlist, _data, i);
    i += SerDeUtil.writeVector(4, marketPolicies, _data, i);
    return i - _offset;
  }

  @Override
  public int l() {
    return SerDeUtil.lenVector(4, collateralAllowlist) + SerDeUtil.lenVector(4, principalAllowlist) + SerDeUtil.lenVector(4, marketPolicies);
  }
}
